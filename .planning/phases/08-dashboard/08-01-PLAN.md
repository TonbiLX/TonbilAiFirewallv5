---
phase: 08-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/gradle/libs.versions.toml
  - android/app/build.gradle.kts
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiRoutes.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DashboardDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/WebSocketDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/WebSocketManager.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/repository/DashboardRepository.kt
  - android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "DashboardRepository.getSummary() backend'den dashboard ozet verisi donduruyor"
    - "WebSocketManager baglanti kurup 3 saniyede bir realtime_update mesaji aliyor"
    - "WebSocketManager lifecycle-aware: pause'da baglantiyi kesiyor, resume'da yeniden kuruyor"
    - "Tum DTO'lar backend JSON yapisina birebir esleniyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DashboardDto.kt"
      provides: "Dashboard summary REST response DTO'lari"
      contains: "DashboardSummaryDto"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/WebSocketDto.kt"
      provides: "WebSocket realtime_update mesaj DTO'lari"
      contains: "RealtimeUpdateDto"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/WebSocketManager.kt"
      provides: "Lifecycle-safe WebSocket baglanti yoneticisi"
      contains: "WebSocketManager"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/repository/DashboardRepository.kt"
      provides: "Dashboard veri katmani (REST + WebSocket)"
      contains: "DashboardRepository"
  key_links:
    - from: "DashboardRepository"
      to: "HttpClient"
      via: "Ktor GET dashboard/summary"
      pattern: "client\\.get.*DASHBOARD_SUMMARY"
    - from: "WebSocketManager"
      to: "wss://wall.tonbilx.com/api/v1/ws"
      via: "Ktor WebSocket client with token query param"
      pattern: "webSocket.*ws\\?token"
    - from: "AppModule"
      to: "WebSocketManager + DashboardRepository"
      via: "Koin singleton registration"
      pattern: "single.*WebSocketManager|single.*DashboardRepository"
---

<objective>
Dashboard veri katmanini olustur: REST DTO'lari, WebSocket DTO'lari, lifecycle-aware WebSocket yoneticisi ve DashboardRepository.

Purpose: Phase 8 UI katmani (Plan 02) icin gerekli tum veri kaynaklarini ve tip tanimlarini saglar. WebSocket baglantisi uygulamanin arka plana gidip gelmesinde otomatik yeniden baglanir.
Output: DTO dosyalari, WebSocketManager, DashboardRepository, Vico + ktor-websockets bagimliliklari
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-authentication/07-02-SUMMARY.md

<interfaces>
<!-- Mevcut kod arabirimleri — executor bunlari dogrudan kullanacak -->

From data/remote/ApiRoutes.kt:
```kotlin
object ApiRoutes {
    const val BASE_URL = "https://wall.tonbilx.com/api/v1/"
    const val LOCAL_URL = "http://192.168.1.2/api/v1/"
    const val DASHBOARD_SUMMARY = "dashboard/summary"
    const val WS_URL = "wss://wall.tonbilx.com/api/v1/ws"
}
```

From data/remote/ApiClient.kt:
```kotlin
fun createHttpClient(serverDiscovery: ServerDiscovery, tokenManager: TokenManager): HttpClient
```

From data/remote/ServerDiscovery.kt:
```kotlin
class ServerDiscovery(serverConfig: ServerConfig, testClient: HttpClient) {
    var activeUrl: String = ApiRoutes.BASE_URL
}
```

From data/local/TokenManager.kt:
```kotlin
class TokenManager(context: Context) {
    fun getToken(): String?
    fun isLoggedIn(): Boolean
}
```

From di/AppModule.kt:
```kotlin
val appModule = module {
    single { TokenManager(androidContext()) }
    single { ServerConfig(androidContext()) }
    single(named("test")) { createTestHttpClient() }
    single { ServerDiscovery(get<ServerConfig>(), get<HttpClient>(named("test"))) }
    single { createHttpClient(get<ServerDiscovery>(), get<TokenManager>()) }
    single { AuthRepository(get(), get<TokenManager>()) }
}
val featureModules = module {
    viewModel { DashboardViewModel(get()) }
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vico + ktor-websockets bagimliliklari ve Dashboard/WebSocket DTO'lari</name>
  <files>
    android/gradle/libs.versions.toml,
    android/app/build.gradle.kts,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DashboardDto.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/WebSocketDto.kt
  </files>
  <action>
1. libs.versions.toml'a ekle:
   - versions bolumune: `vico = "2.1.2"`, `ktorClientWebsockets = "3.4.0"` (ktor versiyonunu ref olarak kullan: version.ref = "ktor")
   - libraries bolumune:
     ```
     vico-compose-m3 = { group = "com.patrykandpatrick.vico", name = "compose-m3", version.ref = "vico" }
     ktor-client-websockets = { group = "io.ktor", name = "ktor-client-websockets", version.ref = "ktor" }
     ```

2. build.gradle.kts dependencies bolumune ekle:
   ```
   // Charting
   implementation(libs.vico.compose.m3)
   // Ktor WebSocket
   implementation(libs.ktor.client.websockets)
   ```

3. `data/remote/dto/DashboardDto.kt` olustur — Backend GET /api/v1/dashboard/summary yanitina birebir eslesir:
   ```kotlin
   @Serializable
   data class DashboardSummaryDto(
       val devices: DevicesSummaryDto,
       val dns: DnsSummaryDto,
       val vpn: VpnSummaryDto,
       @SerialName("top_clients") val topClients: List<TopClientDto>,
       @SerialName("top_queried_domains") val topQueriedDomains: List<TopDomainDto>,
       @SerialName("top_blocked_domains") val topBlockedDomains: List<TopDomainDto>,
   )

   @Serializable
   data class DevicesSummaryDto(val total: Int, val online: Int, val blocked: Int)

   @Serializable
   data class DnsSummaryDto(
       @SerialName("total_queries_24h") val totalQueries24h: Int,
       @SerialName("blocked_queries_24h") val blockedQueries24h: Int,
       @SerialName("block_percentage") val blockPercentage: Float,
       @SerialName("active_blocklists") val activeBlocklists: Int,
       @SerialName("total_blocked_domains") val totalBlockedDomains: Int,
   )

   @Serializable
   data class VpnSummaryDto(
       val enabled: Boolean,
       @SerialName("total_peers") val totalPeers: Int,
       @SerialName("connected_peers") val connectedPeers: Int,
       @SerialName("total_rx") val totalRx: Long,
       @SerialName("total_tx") val totalTx: Long,
   )

   @Serializable
   data class TopClientDto(@SerialName("client_ip") val clientIp: String, @SerialName("query_count") val queryCount: Int)

   @Serializable
   data class TopDomainDto(val domain: String, val count: Int)
   ```

4. `data/remote/dto/WebSocketDto.kt` olustur — Backend WebSocket /api/v1/ws mesaj yapisina birebir eslesir:
   ```kotlin
   @Serializable
   data class RealtimeUpdateDto(
       val type: String,
       @SerialName("device_count") val deviceCount: Int,
       val devices: List<WsDeviceDto>,
       val dns: WsDnsDto,
       val bandwidth: WsBandwidthDto,
       val vpn: WsVpnDto,
       @SerialName("vpn_client") val vpnClient: WsVpnClientDto? = null,
   )

   @Serializable
   data class WsDeviceDto(
       val id: Int,
       val mac: String,
       val ip: String,
       val hostname: String? = null,
       val manufacturer: String? = null,
       @SerialName("is_online") val isOnline: Boolean,
   )

   @Serializable
   data class WsDnsDto(
       @SerialName("total_queries_24h") val totalQueries24h: Int,
       @SerialName("blocked_queries_24h") val blockedQueries24h: Int,
       @SerialName("block_percentage") val blockPercentage: Float,
       @SerialName("queries_per_min") val queriesPerMin: Int,
   )

   @Serializable
   data class WsBandwidthDto(
       @SerialName("total_upload_bps") val totalUploadBps: Long,
       @SerialName("total_download_bps") val totalDownloadBps: Long,
       val devices: Map<String, WsDeviceBandwidthDto> = emptyMap(),
   )

   @Serializable
   data class WsDeviceBandwidthDto(
       @SerialName("upload_bps") val uploadBps: Long,
       @SerialName("download_bps") val downloadBps: Long,
       @SerialName("upload_total") val uploadTotal: Long,
       @SerialName("download_total") val downloadTotal: Long,
   )

   @Serializable
   data class WsVpnDto(
       val enabled: Boolean,
       @SerialName("connected_peers") val connectedPeers: Int,
       @SerialName("total_peers") val totalPeers: Int,
   )

   @Serializable
   data class WsVpnClientDto(
       val connected: Boolean,
       @SerialName("transfer_rx") val transferRx: Long,
       @SerialName("transfer_tx") val transferTx: Long,
   )
   ```

Tum siniflar `kotlinx.serialization.Serializable` annotasyonu ile. Package: `com.tonbil.aifirewall.data.remote.dto`.
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && grep -q "vico" gradle/libs.versions.toml && grep -q "ktor-client-websockets" gradle/libs.versions.toml && test -f app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DashboardDto.kt && test -f app/src/main/java/com/tonbil/aifirewall/data/remote/dto/WebSocketDto.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>libs.versions.toml'da vico-compose-m3 ve ktor-client-websockets tanimli, build.gradle.kts'de implementation satirlari var, DashboardDto.kt 6 data class iceriyor (DashboardSummaryDto, DevicesSummaryDto, DnsSummaryDto, VpnSummaryDto, TopClientDto, TopDomainDto), WebSocketDto.kt 7 data class iceriyor (RealtimeUpdateDto, WsDeviceDto, WsDnsDto, WsBandwidthDto, WsDeviceBandwidthDto, WsVpnDto, WsVpnClientDto)</done>
</task>

<task type="auto">
  <name>Task 2: WebSocketManager + DashboardRepository + Koin wiring</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/WebSocketManager.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/repository/DashboardRepository.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiRoutes.kt,
    android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
  </files>
  <action>
1. ApiRoutes.kt'ye WS URL builder fonksiyonu ekle:
   ```kotlin
   // Mevcut WS_URL sabitinin yanina:
   fun wsUrl(serverDiscovery: ServerDiscovery, token: String): String {
       // activeUrl'den ws URL'i turet: https:// -> wss://, http:// -> ws://
       val baseUrl = serverDiscovery.activeUrl
       val wsBase = baseUrl
           .replace("https://", "wss://")
           .replace("http://", "ws://")
           .removeSuffix("/")
       return "${wsBase}ws?token=$token"
   }
   ```

2. `data/remote/WebSocketManager.kt` olustur — Lifecycle-aware WebSocket yoneticisi:
   ```kotlin
   class WebSocketManager(
       private val client: HttpClient,
       private val serverDiscovery: ServerDiscovery,
       private val tokenManager: TokenManager,
   ) {
       private val json = Json { ignoreUnknownKeys = true; isLenient = true }

       private val _messages = MutableSharedFlow<RealtimeUpdateDto>(
           replay = 1,
           extraBufferCapacity = 5,
           onBufferOverflow = BufferOverflow.DROP_OLDEST,
       )
       val messages: SharedFlow<RealtimeUpdateDto> = _messages.asSharedFlow()

       private val _connectionState = MutableStateFlow(WebSocketState.DISCONNECTED)
       val connectionState: StateFlow<WebSocketState> = _connectionState.asStateFlow()

       private var wsJob: Job? = null
       private var scope: CoroutineScope? = null

       fun connect(scope: CoroutineScope) {
           this.scope = scope
           reconnect()
       }

       fun disconnect() {
           wsJob?.cancel()
           wsJob = null
           _connectionState.value = WebSocketState.DISCONNECTED
       }

       private fun reconnect() {
           wsJob?.cancel()
           val currentScope = scope ?: return
           val token = tokenManager.getToken() ?: return

           wsJob = currentScope.launch {
               while (isActive) {
                   try {
                       _connectionState.value = WebSocketState.CONNECTING
                       val wsUrl = ApiRoutes.wsUrl(serverDiscovery, token)
                       client.webSocket(wsUrl) {
                           _connectionState.value = WebSocketState.CONNECTED
                           for (frame in incoming) {
                               if (frame is Frame.Text) {
                                   try {
                                       val update = json.decodeFromString<RealtimeUpdateDto>(frame.readText())
                                       if (update.type == "realtime_update") {
                                           _messages.emit(update)
                                       }
                                   } catch (_: Exception) { /* skip malformed */ }
                               }
                           }
                       }
                   } catch (_: Exception) {
                       _connectionState.value = WebSocketState.DISCONNECTED
                   }
                   // Reconnect after 3 seconds on disconnect
                   delay(3000)
               }
           }
       }
   }

   enum class WebSocketState { DISCONNECTED, CONNECTING, CONNECTED }
   ```

   Import'lar: io.ktor.client.plugins.websocket.webSocket, io.ktor.websocket.Frame, kotlinx.coroutines.*, kotlinx.coroutines.flow.*, kotlinx.serialization.json.Json.

3. `data/repository/DashboardRepository.kt` olustur:
   ```kotlin
   class DashboardRepository(private val client: HttpClient) {
       suspend fun getSummary(): Result<DashboardSummaryDto> {
           return try {
               val response: DashboardSummaryDto = client.get(ApiRoutes.DASHBOARD_SUMMARY).body()
               Result.success(response)
           } catch (e: Exception) {
               Result.failure(e)
           }
       }
   }
   ```

   Import'lar: io.ktor.client.call.body, io.ktor.client.request.get.

4. AppModule.kt'yi guncelle — appModule'e WebSocketManager ve DashboardRepository singleton ekle:
   ```kotlin
   // appModule icine:
   single { WebSocketManager(get(), get<ServerDiscovery>(), get<TokenManager>()) }
   single { DashboardRepository(get()) }
   ```

   featureModules'daki DashboardViewModel satirini HENUZ degistirme — Plan 02'de guncellenecek.

   Yeni import'lar: WebSocketManager, DashboardRepository.
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && grep -q "WebSocketManager" app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt && grep -q "DashboardRepository" app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt && grep -q "WebSocketState" app/src/main/java/com/tonbil/aifirewall/data/remote/WebSocketManager.kt && grep -q "getSummary" app/src/main/java/com/tonbil/aifirewall/data/repository/DashboardRepository.kt && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>WebSocketManager SharedFlow ile RealtimeUpdateDto yayinliyor, connect/disconnect metodlari var, 3sn reconnect mantigi calisiyor. DashboardRepository.getSummary() Result donduruyor. AppModule'de her ikisi Koin singleton olarak kayitli. ApiRoutes.wsUrl() activeUrl'den dinamik WS URL uretiyor.</done>
</task>

</tasks>

<verification>
- libs.versions.toml'da vico-compose-m3 ve ktor-client-websockets tanimli
- build.gradle.kts'de her iki implementation satiri mevcut
- DashboardDto.kt backend GET /api/v1/dashboard/summary yanitina tam eslesir (6 data class)
- WebSocketDto.kt backend WS /api/v1/ws mesajina tam eslesir (7 data class)
- WebSocketManager lifecycle-aware: connect(scope) ve disconnect() cagrilari ile kontrol edilir
- WebSocketManager reconnect mantigi: baglanti kopunca 3sn sonra tekrar dener
- DashboardRepository.getSummary() Result pattern ile hata yonetimi yapar
- AppModule.kt'de WebSocketManager ve DashboardRepository singleton olarak kayitli
</verification>

<success_criteria>
- Tum DTO siniflarinin backend JSON alan adlariyla @SerialName eslesmesi dogru
- WebSocketManager.messages SharedFlow'u toplandiktan sonra RealtimeUpdateDto yayinliyor
- WebSocketManager.connectionState DISCONNECTED/CONNECTING/CONNECTED arasinda gecis yapiyor
- DashboardRepository HttpClient uzerinden dashboard/summary endpoint'ini basariyla cagiriyor
- Koin'de tum yeni siniflar kayitli ve inject edilebilir durumda
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard/08-01-SUMMARY.md`
</output>
