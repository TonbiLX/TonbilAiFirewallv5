---
phase: 10-dns-filtering
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - android/app/src/main/java/com/tonbil/aifirewall/feature/dnsfiltering/DnsFilteringScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/profiles/ProfilesScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/network/NetworkHubScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
  - android/app/src/main/java/com/tonbil/aifirewall/ui/navigation/AppNavHost.kt
autonomous: true
requirements:
  - DNS-01
  - DNS-02
  - DNS-03
  - DNS-04

must_haves:
  truths:
    - "Kullanici DNS ozet ekraninda toplam sorgu, engelleme sayisi, engelleme orani ve top domain'leri goruyor"
    - "Kullanici tek dokunusla tum DNS guvenlik katmanlarini toplu olarak acip kapatabiliyor (global toggle)"
    - "Kullanici bireysel DNS guvenlik katmanlarini (DNSSEC, Tunneling, DoH) tek tek toggle edebiliyor"
    - "Kullanici DNSSEC modunu segmented button ile degistirebiliyor (enforce/log_only/disabled)"
    - "Kullanici icerik kategorilerini goruntuleyebiliyor ve blocklist baglama yonetimi yapabiliyor"
    - "Kullanici profil olusturabilyor/duzenleyebiliyor — kategori secimi ve bandwidth limiti ayarlayabiliyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/dnsfiltering/DnsFilteringScreen.kt"
      provides: "4-tab DNS Filtering hub ekrani (Ozet, Kategoriler, Profiller, Guvenlik) + global toggle + DNSSEC mode segmented button"
      min_lines: 200
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/profiles/ProfilesScreen.kt"
      provides: "Profil CRUD + content_filters secimi + edit dialog"
      contains: "contentFilters"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt"
      provides: "DnsFilteringViewModel ve guncellenmis ProfilesViewModel Koin DI kaydi"
      contains: "DnsFilteringViewModel"
  key_links:
    - from: "DnsFilteringScreen.kt"
      to: "DnsFilteringViewModel"
      via: "koinViewModel() + uiState collectAsState"
      pattern: "koinViewModel.*DnsFilteringViewModel"
    - from: "DnsFilteringScreen.kt Ozet Tab GlobalToggle"
      to: "DnsFilteringViewModel.toggleGlobalFilter"
      via: "Switch onCheckedChange -> viewModel.toggleGlobalFilter()"
      pattern: "toggleGlobalFilter"
    - from: "DnsFilteringScreen.kt Guvenlik Tab DNSSEC Mode"
      to: "DnsFilteringViewModel.updateDnssecMode"
      via: "SegmentedButton onClick -> viewModel.updateDnssecMode(mode)"
      pattern: "updateDnssecMode"
    - from: "DnsFilteringScreen.kt Tab 1"
      to: "ContentCategoriesScreen"
      via: "Dogrudan composable cagri"
      pattern: "ContentCategoriesScreen"
    - from: "DnsFilteringScreen.kt Tab 2"
      to: "ProfilesScreen"
      via: "Dogrudan composable cagri"
      pattern: "ProfilesScreen"
    - from: "NetworkHubScreen.kt"
      to: "DnsFilteringRoute"
      via: "HubItem navigasyon"
      pattern: "DnsFilteringRoute"
    - from: "AppNavHost.kt"
      to: "DnsFilteringScreen"
      via: "composable(DnsFilteringRoute) route binding"
      pattern: "DnsFilteringRoute"
---

<objective>
DNS Filtering UI katmani: 4-tab DnsFilteringScreen (global toggle + DNSSEC mode segmented button dahil), ProfilesScreen content_filters genisletmesi, navigasyon wiring ve Koin DI.

Purpose: Plan 01'de hazirlanan veri katmanini kullanici arayuzune baglamak. Kullanicinin DNS filtreleme sistemini mobil uzerinden tam olarak gorebilmesi ve yonetebilmesi. Global DNS toggle (DNS-02) ve DNSSEC mode secimi icin UI saglamak.
Output: DnsFilteringScreen.kt (4-tab hub + global toggle + DNSSEC mode), guncellenmis ProfilesScreen.kt, guncellenmis AppNavHost + NetworkHubScreen + AppModule
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-dns-filtering/10-CONTEXT.md
@.planning/phases/10-dns-filtering/10-RESEARCH.md
@.planning/phases/10-dns-filtering/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01'den uretilen tipler — executor bunlari kullanmali -->

From DnsFilteringViewModel.kt (Plan 01 uretimi):
```kotlin
data class DnsFilteringUiState(
    val stats: DnsStatsDto? = null,
    val securityConfig: SecurityConfigDto? = null,
    val isLoading: Boolean = true,
    val isTogglingFilter: Boolean = false,
    val error: String? = null,
    val actionMessage: String? = null,
    val selectedTab: Int = 0,
)

class DnsFilteringViewModel(private val securityRepository: SecurityRepository) : ViewModel() {
    val uiState: StateFlow<DnsFilteringUiState>
    val isGlobalFilterActive: Boolean  // computed: tum katmanlar acik mi?
    fun loadAll()
    fun selectTab(index: Int)
    fun toggleGlobalFilter(enabled: Boolean)  // DNS-02: tum katmanlari toplu ac/kapa
    fun toggleDnssec(enabled: Boolean)
    fun toggleDnsTunneling(enabled: Boolean)
    fun toggleDoh(enabled: Boolean)
    fun toggleDga(enabled: Boolean)
    fun toggleSinkhole(enabled: Boolean)
    fun updateRateLimit(perSec: Int)
    fun updateDnssecMode(mode: String)  // enforce/log_only/disabled
    fun clearActionMessage()
}
```

From ProfilesViewModel.kt (Plan 01 guncelleme):
```kotlin
data class ProfilesUiState(
    val profiles: List<ProfileResponseDto> = emptyList(),
    val categories: List<ContentCategoryDto> = emptyList(),
    val isLoading: Boolean = true,
    val error: String? = null,
    val actionMessage: String? = null,
    val showAddDialog: Boolean = false,
    val addName: String = "",
    val addBandwidth: String = "",
    val addContentFilters: List<String> = emptyList(),
    val isAdding: Boolean = false,
    val deleteTarget: ProfileResponseDto? = null,
    val isDeleting: Boolean = false,
    val editTarget: ProfileResponseDto? = null,
)

class ProfilesViewModel(repo: SecurityRepository, categoryRepo: ContentCategoryRepository) {
    fun toggleContentFilter(categoryKey: String)
    fun showEditDialog(profile: ProfileResponseDto)
    fun updateProfile()
    // ... mevcut fonksiyonlar
}
```

From SecurityConfigDto (Plan 01 guncelleme):
```kotlin
data class SecurityConfigDto(
    val threatAnalysis: ThreatAnalysisConfigDto,
    val dnsSecurity: DnsSecurityConfigDto,
    val alertSettings: AlertSettingsConfigDto,
    val dnssecEnabled: Boolean = true,
    val dnssecMode: String = "log_only",
    val dnsTunnelingEnabled: Boolean = true,
    val dohEnabled: Boolean = true,
    // ... tunneling detail fields
)
```

From SecurityConfigUpdateDto (Plan 01 guncelleme):
```kotlin
data class SecurityConfigUpdateDto(
    // ... mevcut alanlar ...
    val dnssecEnabled: Boolean? = null,
    val dnssecMode: String? = null,  // enforce/log_only/disabled
    val dnsTunnelingEnabled: Boolean? = null,
    val dohEnabled: Boolean? = null,
    // ...
)
```

From DnsStatsDto (Plan 01 guncelleme):
```kotlin
data class DnsStatsDto(
    val totalQueries24h: Int = 0,
    val blockedQueries24h: Int = 0,
    val blockPercentage: Float = 0f,
    val totalBlocklistDomains: Int = 0,
    val activeBlocklists: Int = 0,
    val topBlockedDomains: List<TopDomainDto> = emptyList(),
    val topQueriedDomains: List<TopDomainDto> = emptyList(),
    val externalQueries24h: Int = 0,
    val dotQueries24h: Int = 0,
    val internalQueries24h: Int = 0,
)
```

From NavRoutes.kt (Plan 01 ekleme):
```kotlin
@Serializable object DnsFilteringRoute
```

From NetworkHubScreen.kt (mevcut):
```kotlin
// DnsBlockingRoute kullaniliyor — DnsFilteringRoute ile degistirilecek
HubItem("DNS Engelleme", Icons.Outlined.Shield, NeonCyan, DnsBlockingRoute)
```

Mevcut Proje Patternleri:
- ScrollableTabRow + Tab: SecuritySettingsScreen.kt ornegi
- koinViewModel(): tum ekranlar
- collectAsStateWithLifecycle(): tum ekranlar
- GlassCard: ortak kart wrapper
- SnackbarHostState: aksiyon mesajlari
- SwipeRefresh/pullRefresh: pull-to-refresh
- CyberpunkColors: LocalCyberpunkColors.current
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DnsFilteringScreen 4-tab hub ekrani</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/dnsfiltering/DnsFilteringScreen.kt
  </files>
  <action>
Yeni `feature/dnsfiltering/DnsFilteringScreen.kt` dosyasi olustur. 4-tab yapisinda DNS Filtering hub ekrani:

**Genel yapi:**
- `@Composable fun DnsFilteringScreen(onBack: () -> Unit, viewModel: DnsFilteringViewModel = koinViewModel())`
- `val uiState by viewModel.uiState.collectAsStateWithLifecycle()`
- Scaffold + SnackbarHost + pullRefresh (proje standardi)
- onBack callback'i bu task icinde Scaffold topBar'a geri butonu olarak ekle (NavigateUp icon + onBack() cagri)

**Tab yapisi:** ScrollableTabRow (SecuritySettingsScreen.kt pattern'ini BIREBIR kopyala):
```kotlin
val tabTitles = listOf("Ozet", "Kategoriler", "Profiller", "Guvenlik")
ScrollableTabRow(
    selectedTabIndex = uiState.selectedTab,
    containerColor = Color.Transparent,
    contentColor = colors.neonCyan,
    edgePadding = 0.dp,
) {
    tabTitles.forEachIndexed { index, title ->
        Tab(
            selected = uiState.selectedTab == index,
            onClick = { viewModel.selectTab(index) },
            text = { Text(title, fontSize = 13.sp) },
        )
    }
}
```

**Tab 0 — Ozet:**
1. Global DNS Filtreleme Toggle (GlassCard icinde, EN USTTE):
   - Baslik: "DNS Filtreleme" + Switch
   - Switch durumu: `viewModel.isGlobalFilterActive`
   - Switch onCheckedChange: `viewModel.toggleGlobalFilter(!viewModel.isGlobalFilterActive)`
   - Aciklama metni: "Tum DNS guvenlik katmanlarini tek tusla ac/kapa"
   - Switch disabled: `uiState.isTogglingFilter`

2. DNS Istatistik Kartlari (GlassCard icinde):
   - Toplam Sorgu (totalQueries24h) — NeonCyan
   - Engellenen (blockedQueries24h) — NeonRed
   - Engelleme Orani (blockPercentage) — NeonAmber
   - Aktif Blocklist (activeBlocklists) — NeonGreen
   Her biri StatItem seklinde: buyuk sayi + kucuk etiket

3. Kaynak Tipi Dagilimi (GlassCard):
   - Row icinde 3 chip: Internal (internalQueries24h), External (externalQueries24h), DoT (dotQueries24h)
   - Sayi 0 ise gri, >0 ise ilgili neon renk

4. DNS Guvenlik Katmanlari (GlassCard "Guvenlik Katmanlari" baslikli):
   Her satir icin SecurityLayerRow composable (8dp yuvarlak indicator + label + detay + Switch):
   - DNSSEC Dogrulama: dnssecEnabled + dnssecMode gosterimi (enforce/log_only/disabled), Switch toggle
   - DNS Tunneling Tespiti: dnsTunnelingEnabled, Switch toggle
   - DoH Endpoint: dohEnabled + "guard.tonbilx.com" detay, Switch toggle
   - DGA Tespiti: threatAnalysis.dgaDetectionEnabled, Switch toggle
   - Sinkhole: dnsSecurity.sinkholeEnabled + sinkholeIp detay, Switch toggle
   - Rate Limiting: dnsSecurity.rateLimitEnabled + "${rateLimitPerSecond}/sn" detay
   Toggle'lar `viewModel.toggleDnssec()`, `toggleDnsTunneling()`, `toggleDoh()`, `toggleDga()`, `toggleSinkhole()` cagirsin

5. En Cok Sorgulanan Domainler (GlassCard):
   - topQueriedDomains listesi: domain + count
   - Bos ise "Henuz veri yok" placeholder

6. En Cok Engellenen Domainler (GlassCard):
   - topBlockedDomains listesi: domain + count, NeonRed vurgu
   - Bos ise "Henuz veri yok" placeholder

**Tab 1 — Kategoriler:**
- Dogrudan `ContentCategoriesScreen()` composable'ini cagir (mevcut, calisiyor)
- ContentCategoriesViewModel zaten Koin'de kayitli

**Tab 2 — Profiller:**
- Dogrudan `ProfilesScreen()` composable'ini cagir (Plan 01'de guncellenmis hali)
- ProfilesViewModel zaten Koin'de kayitli (guncellenmis olacak)

**Tab 3 — Guvenlik:**
- SecurityConfig detayli ayarlar (DnsSecurityConfigDto alanlari):
  - Rate Limit: Switch + sayi input (rateLimitPerSecond)
  - Engelli Sorgu Tipleri: blockedQueryTypes listesi chip'lerle
  - Sinkhole IPv4: Text field gosterim (sinkholeIp)
  - DNSSEC Modu: 3-option segmented button (enforce/log_only/disabled). SingleChoiceSegmentedButtonRow kullan:
    ```kotlin
    val dnssecModes = listOf("enforce", "log_only", "disabled")
    val currentMode = uiState.securityConfig?.dnssecMode ?: "log_only"
    SingleChoiceSegmentedButtonRow {
        dnssecModes.forEachIndexed { index, mode ->
            SegmentedButton(
                selected = currentMode == mode,
                onClick = { viewModel.updateDnssecMode(mode) },
                shape = SegmentedButtonDefaults.itemShape(index = index, count = dnssecModes.size),
            ) {
                Text(
                    when (mode) {
                        "enforce" -> "Enforce"
                        "log_only" -> "Log Only"
                        else -> "Devre Disi"
                    },
                    fontSize = 12.sp,
                )
            }
        }
    }
    ```
  - Tunneling Parametreleri: maxSubdomainLen, maxLabelsPerMin, txtRatioThreshold gosterim
- Her degisiklik viewModel uzerinden SecurityConfig update cagrisi yapsin

**Stil:** Cyberpunk tema (DarkBackground, GlassBg, NeonCyan/Magenta/Green vurgular). Tum kartlar GlassCard icinde. Proje geneli tutarli.
  </action>
  <verify>
    <automated>cd E:\Nextcloud-Yeni\TonbilAiFirevallv5\android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>DnsFilteringScreen 4-tab goruntuluyor: Ozet tab'inda global DNS toggle + DNS stats + guvenlik katmanlari + top domain'ler, Kategoriler tab'i ContentCategoriesScreen, Profiller tab'i ProfilesScreen, Guvenlik tab'i detayli DNS config (DNSSEC mode segmented button dahil). onBack geri butonu Scaffold topBar'da mevcut. APK derleniyor.</done>
</task>

<task type="auto">
  <name>Task 2: ProfilesScreen genisletme + navigasyon wiring + Koin DI</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/profiles/ProfilesScreen.kt,
    android/app/src/main/java/com/tonbil/aifirewall/feature/network/NetworkHubScreen.kt,
    android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt,
    android/app/src/main/java/com/tonbil/aifirewall/ui/navigation/AppNavHost.kt
  </files>
  <action>
1. ProfilesScreen.kt — Mevcut AddProfileDialog'u genislet:
   - Profil Adi (mevcut TextField)
   - Bandwidth Limiti (mevcut TextField)
   - Icerik Filtreleri (YENi — kategori secimi):
     ```kotlin
     Text("Icerik Filtreleri", color = TextPrimary, fontWeight = FontWeight.SemiBold)
     // Kategorileri FlowRow veya LazyColumn icinde Checkbox + Text satirlari olarak listele
     state.categories.forEach { category ->
         val isChecked = category.key in state.addContentFilters
         Row(
             modifier = Modifier.clickable { viewModel.toggleContentFilter(category.key) },
             verticalAlignment = Alignment.CenterVertically,
         ) {
             Checkbox(
                 checked = isChecked,
                 onCheckedChange = { viewModel.toggleContentFilter(category.key) },
                 colors = CheckboxDefaults.colors(checkedColor = parseHexColor(category.color)),
             )
             Text(category.name, color = TextPrimary)
             if (category.domainCount > 0) {
                 Text(" (${category.domainCount})", color = TextSecondary, fontSize = 11.sp)
             }
         }
     }
     ```
   - Dialog basligini dinamik yap: `editTarget != null` ise "Profil Duzenle", yoksa "Yeni Profil"
   - Kaydet butonunu dinamik yap: `editTarget != null` ise `viewModel.updateProfile()`, yoksa `viewModel.createProfile()`

   - Profil kartlarinda "Duzenle" butonu ekle (her ProfileCard'da):
     ```kotlin
     IconButton(onClick = { viewModel.showEditDialog(profile) }) {
         Icon(Icons.Outlined.Edit, "Duzenle", tint = NeonCyan)
     }
     ```
   - Profil kartinda content_filters gosterimi: profile.contentFilters bos degilse chip'lerle goster

   - parseHexColor fonksiyonu ContentCategoriesScreen'den import et (veya ayni dosyada tanimla)

2. NetworkHubScreen.kt — DnsBlockingRoute yerine DnsFilteringRoute kullan:
   ```kotlin
   // Eski: HubItem("DNS Engelleme", Icons.Outlined.Shield, NeonCyan, DnsBlockingRoute)
   // Yeni: HubItem("DNS Filtreleme", Icons.Outlined.Shield, NeonCyan, DnsFilteringRoute)
   ```
   Import'u guncelle: `DnsBlockingRoute` → `DnsFilteringRoute`

   Ayrica "Icerik Filtreleri" item'ini kaldir (artik DNS Filtreleme icinde tab olarak var):
   ```kotlin
   // KALDIR: HubItem("Icerik Filtreleri", Icons.Outlined.FilterList, NeonRed, ContentCategoriesRoute)
   ```

   Profiller item'ini da ekle (veya mevcut ProfilesRoute'a yonlendiren bir item varsa kaldir — Profiller artik DNS Filtreleme icinde):
   Eger NetworkHubScreen'de ProfilesRoute yoksa degisiklik yapma.

3. AppModule.kt — DnsFilteringViewModel Koin kaydini ekle:
   ```kotlin
   // featureModules icine:
   import com.tonbil.aifirewall.feature.dnsfiltering.DnsFilteringViewModel
   viewModelOf(::DnsFilteringViewModel)
   ```

   ProfilesViewModel Koin kaydini guncelle (ContentCategoryRepository eklendi):
   ```kotlin
   // Eski: viewModelOf(::ProfilesViewModel)
   // Yeni: viewModel { ProfilesViewModel(get<SecurityRepository>(), get<ContentCategoryRepository>()) }
   ```

4. AppNavHost.kt — DnsFilteringRoute composable ekle:
   ```kotlin
   import com.tonbil.aifirewall.feature.dnsfiltering.DnsFilteringScreen
   import com.tonbil.aifirewall.ui.navigation.DnsFilteringRoute

   composable<DnsFilteringRoute> {
       DnsFilteringScreen(onBack = { navController.popBackStack() })
   }
   ```

   DnsBlockingRoute composable'i KALDIRILMASIN — ileride gerekebilir. Sadece NetworkHubScreen'deki link'i DnsFilteringRoute'a cevir.
  </action>
  <verify>
    <automated>cd E:\Nextcloud-Yeni\TonbilAiFirevallv5\android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>ProfilesScreen'de content_filters secimi + edit dialog calisiyor. NetworkHubScreen'de "DNS Filtreleme" linki DnsFilteringScreen'e yonlendiriyor. Koin DI tum ViewModel'leri inject ediyor. AppNavHost DnsFilteringRoute'u DnsFilteringScreen'e bagliyor. APK derleniyor.</done>
</task>

</tasks>

<verification>
- `./gradlew assembleDebug` basarili (derleme hatasi yok)
- DnsFilteringScreen 4 tab iceriyor: Ozet, Kategoriler, Profiller, Guvenlik
- Ozet tab: Global DNS filtreleme toggle (tek tusla tum katmanlari ac/kapa) + DNS stats kartlari + guvenlik katmanlari (DNSSEC/Tunneling/DoH/DGA) durumu + toggle'lar + top domain listesi
- Guvenlik tab: DNSSEC mode segmented button (enforce/log_only/disabled) + detayli DNS guvenlik ayarlari
- Kategoriler tab: Mevcut ContentCategoriesScreen calisiyor
- Profiller tab: Profil CRUD + content_filters secimi + edit dialog
- NetworkHubScreen'den DnsFilteringScreen'e navigasyon calisiyor
- DnsFilteringScreen onBack geri butonu Scaffold topBar'da mevcut
</verification>

<success_criteria>
1. DNS-01: DNS ozet ekraninda toplam sorgu, engelleme sayisi, en cok sorgulanan ve engellenen domainler listeleniyor
2. DNS-02: Ozet tab'inda global DNS filtreleme toggle'i tum katmanlari tek dokunusla acip kapatabiliyor + bireysel katman toggle'lari da calisiyor
3. DNS-03: Kategoriler tab'inda icerik kategorileri goruntulenebiliyor ve blocklist baglama yonetimi yapilabiliyor
4. DNS-04: Profiller tab'inda profil olusturulabiliyor/duzenlenebiliyor — kategori secimi ve bandwidth limiti ayarlanabiliyor
5. Guvenlik tab'inda DNSSEC modu segmented button ile degistirilebiliyor (enforce/log_only/disabled)
6. APK hatasiz derleniyor ve NetworkHubScreen'den DNS Filtreleme'ye navigasyon calisiyor
</success_criteria>

<output>
After completion, create `.planning/phases/10-dns-filtering/10-02-SUMMARY.md`
</output>
