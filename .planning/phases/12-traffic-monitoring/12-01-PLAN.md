---
phase: 12-traffic-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficViewModel.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt
autonomous: true
requirements:
  - TRAF-01
  - TRAF-02
  - TRAF-03
  - TRAF-04
must_haves:
  truths:
    - "Canli akislar tab'i 5 saniyede bir otomatik guncelleniyor, per-flow baglanti listesi gorunuyor"
    - "Buyuk transferler tab'i 3 saniyede bir guncelleniyor, >10MB flow'lar neon-magenta badge ile vurgulaniyor"
    - "Trafik gecmisi tab'inda sayfalama calisiyor, ileri/geri sayfa gecisi yapiyor"
    - "DeviceDetailScreen'de per-device bant genisligi zaman serisi grafigi (Vico CartesianChart) gorunuyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficScreen.kt"
      provides: "3-tab trafik izleme ekrani (Canli/Buyuk/Gecmis + PerDevice 4. tab)"
      min_lines: 800
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficViewModel.kt"
      provides: "Auto-refresh mantigi ve tab bazli veri yukleme"
      min_lines: 120
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt"
      provides: "Per-device Vico chart bolumu"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt"
      provides: "Bandwidth zaman serisi state (BandwidthPoint listesi)"
  key_links:
    - from: "TrafficScreen.kt"
      to: "TrafficViewModel.kt"
      via: "uiState collectAsState"
      pattern: "viewModel\\.uiState"
    - from: "DeviceDetailScreen.kt"
      to: "DeviceDetailViewModel.kt"
      via: "uiState bandwidth + CartesianChartModelProducer"
      pattern: "CartesianChartModelProducer|modelProducer"
---

<objective>
Trafik izleme ekranini ve per-device grafigini tamamla.

Purpose: TrafficScreen (4-tab) ve TrafficViewModel buyuk olcude implement edilmis durumda. Bu plan gecmis tab sayfalama iyilestirmesi + DeviceDetailScreen'e Vico bant genisligi grafigi ekleyecek. Boylece tum TRAF-* gereksinimleri karsilanmis olacak.

Output: Tam calisan TrafficScreen (auto-refresh, sayfalama) + DeviceDetailScreen Vico chart
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-traffic-monitoring/12-RESEARCH.md

@android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficViewModel.kt
@android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficScreen.kt
@android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt
@android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt
@android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/SecurityDtos.kt
@android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/ExtendedDtos.kt

<interfaces>
<!-- Mevcut DTO'lar (zaten calisiyor, degistirmeye gerek yok) -->

From SecurityDtos.kt:
```kotlin
data class LiveFlowDto(
    @SerialName("flow_id") val flowId: String = "",
    @SerialName("device_id") val deviceId: Int? = null,
    @SerialName("device_hostname") val deviceHostname: String? = null,
    // ... tum flow alanlari mevcut
    @SerialName("bytes_total") val bytesTotal: Long = 0,
    @SerialName("bps_in") val bpsIn: Double = 0.0,
    @SerialName("bps_out") val bpsOut: Double = 0.0,
)
```

From ExtendedDtos.kt:
```kotlin
data class TrafficPerDeviceDto(...)
data class TrafficHistoryDto(
    val items: List<TrafficHistoryItemDto>,
    val total: Int, val page: Int, val pageSize: Int
)
```

From DeviceDetailViewModel.kt:
```kotlin
data class DeviceDetailUiState(
    val bandwidth: WsDeviceBandwidthDto? = null,  // WebSocket'ten canli rx/tx
    val liveFlows: List<LiveFlowDto> = emptyList(),
    // ...
)
```

From DashboardScreen.kt (Vico pattern):
```kotlin
val modelProducer = remember { CartesianChartModelProducer() }
LaunchedEffect(data) {
    modelProducer.runTransaction {
        lineSeries { series(rxPoints, txPoints) }
    }
}
CartesianChartHost(
    chart = rememberCartesianChart(rememberLineCartesianLayer(), ...),
    modelProducer = modelProducer,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TrafficScreen ince ayar + gecmis sayfalama iyilestirmesi</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficViewModel.kt
    android/app/src/main/java/com/tonbil/aifirewall/feature/traffic/TrafficScreen.kt
  </files>
  <action>
TrafficScreen ve TrafficViewModel buyuk olcude implement edilmis durumda (810 + 126 satir). Asagidaki iyilestirmeleri yap:

1. **TrafficViewModel — loadHistory sayfalama ekleme modu:**
   - Mevcut `loadHistory(page)` her seferinde `history` state'ini degistiriyor (sayfa 2 gelince sayfa 1 kayboluyor)
   - Bunu "ekleme modu"na cevir: `history.items + newHistory.items` ile birlestir
   - `historyPage` state'ini koru, `canLoadMore` turet (currentPage < totalPages)
   - NOT: Mevcut sayfalama UI (ileri/geri butonlari) zaten calisiyor, bu sadece opsiyonel iyilestirme. Eger mevcut UI kullanici deneyimi yeterliyse (sayfa degistirme mantigi), degistirmeye GEREK YOK. Sadece mevcut sayfalama hatasiz calistigini dogrula.

2. **TrafficScreen — auto-refresh dogrulama:**
   - Mevcut `LaunchedEffect(uiState.selectedTab)` kontrol et: Tab 0 (canli) 5s, Tab 1 (buyuk) 3s, Tab 2 (gecmis) sayfalama, Tab 3 (per-device) varsa 5s
   - Auto-refresh sadece aktif tab'i yenilemeli (zaten oyle gorundugu icin dogrula)
   - >10MB neon-magenta animasyonlu badge LiveFlowCard icinde zaten var mi kontrol et, yoksa ekle

3. **Domain arama filtresi (TRAF-03 ek gereksinim):**
   - Gecmis tab'a domain/hedef arama TextField ekle (eger yoksa)
   - `OutlinedTextField` ile arama, `filteredItems = sortedItems.filter { it.dstDomain?.contains(query) == true || it.dstIp.contains(query) }`
   - 400ms debounce gereksiz — listeyi aninda filtrele (veriler zaten lokal)

Mevcut kodu kir/boz YAPMA. Sadece eksik ozellikleri ekle. Calisan UI'yi degistirme.
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>TrafficScreen 4-tab auto-refresh calisiyor, gecmis tab sayfalama + arama filtresi mevcut, >10MB badge gorunuyor, assembleDebug basarili</done>
</task>

<task type="auto">
  <name>Task 2: DeviceDetailScreen Vico bant genisligi grafigi (TRAF-04)</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt
    android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt
  </files>
  <action>
DeviceDetailScreen'e per-device bant genisligi zaman serisi grafigi ekle. Backend `/flows/device/{id}/summary` zaman serisi donmuyor — bunun yerine WebSocket'ten gelen `bandwidth` verisini kullan (zaten `DeviceDetailViewModel.uiState.bandwidth` mevcut).

1. **DeviceDetailViewModel — BandwidthPoint biriktirme:**
   - `data class BandwidthPoint(val rx: Float, val tx: Float)` tanimla (veya mevcut DashboardScreen'den import et)
   - `val bandwidthHistory: List<BandwidthPoint>` state ekle (max 60 nokta — 3 dakika @ 3s interval)
   - WS bandwidth collect eden yerde (satir ~53-60 civari), her gelen `WsDeviceBandwidthDto`'dan yeni `BandwidthPoint` ekle: `bandwidthHistory = (old + BandwidthPoint(rx, tx)).takeLast(60)`
   - rx/tx alanlari `WsDeviceBandwidthDto` icindeki `rxBps`/`txBps` veya benzeri alanlardan alinacak — DTO'yu oku ve dogru alan isimlerini kullan

2. **DeviceDetailScreen — Vico CartesianChart bolumu:**
   - Mevcut tab/icerik yapisinin uygun yerine (tercihen "Trafik" tab'i altina veya liveFlows listesinin ustune) bir GlassCard icinde grafik ekle
   - DashboardScreen'deki Vico pattern'i kullan:
     ```kotlin
     val modelProducer = remember { CartesianChartModelProducer() }
     LaunchedEffect(bandwidthHistory) {
         if (bandwidthHistory.isNotEmpty()) {
             modelProducer.runTransaction {
                 lineSeries {
                     series(bandwidthHistory.map { it.rx })
                     series(bandwidthHistory.map { it.tx })
                 }
             }
         }
     }
     ```
   - Grafik baslik: "Bant Genisligi" (NeonCyan)
   - Iki seri: RX (NeonCyan cizgi), TX (NeonMagenta cizgi)
   - Y ekseni: bps biriminde otomatik olcekleme
   - Grafik yuksekligi: 180.dp
   - Eger bandwidthHistory bos ise grafigi gosterme (veri beklenene kadar)

3. **Import'lar:**
   - Vico: `com.patrykandpatrick.vico.compose.cartesian.*`, `com.patrykandpatrick.vico.core.cartesian.data.*`
   - DashboardScreen.kt'teki import listesini referans al

Dikkat: DeviceDetailScreen mevcut tum islevselligini koru. Grafigi ekstra bolum olarak ekle.
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>DeviceDetailScreen'de Vico CartesianChart ile canli bant genisligi grafigi gorunuyor (WS verisi birikince 60 noktaya kadar), RX=cyan TX=magenta cizgiler, assembleDebug basarili</done>
</task>

</tasks>

<verification>
1. `./gradlew assembleDebug` basarili (sifir hata)
2. TrafficScreen: 4 tab (Canli/Buyuk/Gecmis/PerDevice) arasinda gecis calisiyor
3. Canli tab 5s'de bir yenileniyor, Buyuk tab 3s'de bir yenileniyor
4. Gecmis tab sayfalama calisiyor, domain arama filtresi mevcut
5. DeviceDetailScreen'de Vico bant genisligi grafigi WebSocket verisiyle canli guncelleniyor
</verification>

<success_criteria>
- Tum TRAF-* gereksinimleri karsilanmis: canli akislar (TRAF-01), buyuk transferler (TRAF-02), gecmis sayfalama (TRAF-03), per-device grafik (TRAF-04)
- assembleDebug basarili
- Mevcut islevsellikte gerileme yok
</success_criteria>

<output>
After completion, create `.planning/phases/12-traffic-monitoring/12-01-SUMMARY.md`
</output>
