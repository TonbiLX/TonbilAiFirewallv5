---
phase: 01-bridge-isolation-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/hal/linux_nftables.py
autonomous: true
requirements:
  - ISOL-01
  - ISOL-02
  - ISOL-03
  - ISOL-04
  - ISOL-05
  - ISOL-06
  - ISOL-07
  - ROLL-01
  - ROLL-02
  - ROLL-03

must_haves:
  truths:
    - "ensure_bridge_isolation() applies L2 forward drop rules without SSH lockout"
    - "ensure_bridge_isolation() is idempotent — calling twice does not duplicate drop rules"
    - "After ensure_bridge_isolation(), Pi has internet access (NAT MASQUERADE + ip_forward working)"
    - "After remove_bridge_isolation() is called, nft list chain bridge filter forward shows no bridge_isolation rules and the system returns to transparent bridge mode."
    - "remove_bridge_isolation() restores ICMP redirects (send_redirects=1)"
    - "bridge masquerade_fix table is absent after ensure_bridge_isolation() runs"
    - "ensure_bridge_masquerade() is marked deprecated but not removed"
    - "Six sysctl values (ip_forward=1, bridge-nf-call-iptables=1, send_redirects=0 on all+br0) are active before drop rules are applied"
  artifacts:
    - path: "backend/app/hal/linux_nftables.py"
      provides: "ensure_bridge_isolation() and remove_bridge_isolation() functions"
      contains: "async def ensure_bridge_isolation"
    - path: "backend/app/hal/linux_nftables.py"
      provides: "remove_bridge_isolation() rollback function"
      contains: "async def remove_bridge_isolation"
  key_links:
    - from: "ensure_bridge_isolation() Step 4"
      to: "ensure_nat_postrouting_chain()"
      via: "MASQUERADE verified present BEFORE drop rules applied"
      pattern: '"bridge_lan_masq" not in ruleset'
    - from: "ensure_bridge_isolation() Step 5"
      to: "nft -f - stdin batch"
      via: "both drop rules in single atomic transaction"
      pattern: "asyncio.create_subprocess_exec.*NFT_BIN.*-f.*-"
    - from: "remove_bridge_isolation()"
      to: "nft -a list chain bridge filter forward"
      via: "live handle lookup with bridge_isolation comment filter"
      pattern: "handle\\s+(\\d+)"
---

<objective>
Add ensure_bridge_isolation() and remove_bridge_isolation() to linux_nftables.py.

Purpose: Transform Pi from transparent bridge mode (modem sees all LAN device MACs) to router mode (modem sees only Pi's MAC). Provide a safe software rollback path. This is the foundation for all subsequent phases.

Output: Two new async functions in backend/app/hal/linux_nftables.py implementing the full 7-step isolation sequence and handle-based rollback. ensure_bridge_masquerade() marked deprecated (not removed — Phase 4 swaps it in main.py).
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bridge-isolation-core/01-RESEARCH.md
@backend/app/hal/linux_nftables.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ensure_bridge_isolation() to linux_nftables.py</name>
  <files>backend/app/hal/linux_nftables.py</files>
  <action>
Add the async function ensure_bridge_isolation() immediately after the existing ensure_bridge_masquerade() function. Also mark ensure_bridge_masquerade() as deprecated with a docstring note: "DEPRECATED: Use ensure_bridge_isolation() instead. Removed in Phase 4 (main.py lifespan swap)."

The function must follow this exact 7-step sequence — do NOT reorder:

Step 1 — ip_forward=1:
  await _run_system_cmd(["sudo", "sysctl", "-w", "net.ipv4.ip_forward=1"], check=False)

Step 2 — br_netfilter + bridge-nf-call-iptables:
  await _run_system_cmd(["sudo", "modprobe", "br_netfilter"], check=False)
  await _run_system_cmd(["sudo", "sysctl", "-w", "net.bridge.bridge-nf-call-iptables=1"], check=False)

Step 3 — ICMP redirects off (all interfaces + br0):
  await _run_system_cmd(["sudo", "sysctl", "-w", "net.ipv4.conf.all.send_redirects=0"], check=False)
  await _run_system_cmd(["sudo", "sysctl", "-w", "net.ipv4.conf.br0.send_redirects=0"], check=False)

Step 4 — MASQUERADE verified/added BEFORE drop rules:
  wan_iface = await _get_wan_bridge_port()   # returns "eth0" dynamically
  lan_subnet = await _detect_lan_subnet()     # returns "192.168.1.0/24" dynamically
  ruleset = await run_nft(["list", "ruleset"], check=False) or ""
  If "bridge_lan_masq" not in ruleset:
    - Call await ensure_nat_postrouting_chain()
    - Apply via asyncio.create_subprocess_exec("sudo", NFT_BIN, "-f", "-", ...) with stdin:
        add rule inet nat postrouting ip saddr {lan_subnet} ip daddr != {lan_subnet} masquerade comment "bridge_lan_masq"
    - If proc.returncode != 0: logger.error(...) and RETURN immediately (do NOT proceed to drop rules — SSH lockout prevention)
    - Log success with lan_subnet

Step 5 — Drop rules ATOMIC (both directions in one nft -f - transaction):
  Check if "bridge_isolation_lan_wan" not in ruleset OR "bridge_isolation_wan_lan" not in ruleset.
  If either missing, apply BOTH rules together via asyncio.create_subprocess_exec("sudo", NFT_BIN, "-f", "-", ...):
    add rule bridge filter forward iifname "eth1" oifname "{wan_iface}" drop comment "bridge_isolation_lan_wan"
    add rule bridge filter forward iifname "{wan_iface}" oifname "eth1" drop comment "bridge_isolation_wan_lan"
  If proc.returncode != 0: raise RuntimeError(f"Bridge isolation drop rules failed: {stderr}")
  Log: "Bridge isolation: L2 forwarding blocked (eth1<->{wan_iface})"

Step 6 — Delete masquerade_fix table (safe ONLY after drop rules active):
  If BRIDGE_MASQ_TABLE in ruleset:
    await run_nft(["delete", "table", BRIDGE_MASQ_TABLE], check=False)
    Log removal

Step 7 — Persist:
  await persist_nftables()
  Log: "Bridge isolation active — router mode"

The function docstring must state: "Safe to call multiple times (idempotent via comment anchors). Steps ordered to prevent SSH lockout — do not reorder."

Do NOT use check=True on sysctl calls (consistent with existing codebase pattern).
Do NOT hardcode "eth0" or "192.168.1.0/24" — use _get_wan_bridge_port() and _detect_lan_subnet().
Do NOT apply drop rules before MASQUERADE is confirmed in ruleset.
  </action>
  <verify>
    <automated>cd /e/Nextcloud-Yeni/TonbilAiFirewallV41 && python -c "import ast; ast.parse(open('backend/app/hal/linux_nftables.py').read()); print('syntax OK')" && grep -n "async def ensure_bridge_isolation" backend/app/hal/linux_nftables.py && grep -n "bridge_isolation_lan_wan" backend/app/hal/linux_nftables.py && grep -n "bridge_isolation_wan_lan" backend/app/hal/linux_nftables.py && grep -n "bridge_lan_masq" backend/app/hal/linux_nftables.py && grep -n "DEPRECATED" backend/app/hal/linux_nftables.py && python -c "
src = open('backend/app/hal/linux_nftables.py').read()
fn_start = src.find('async def ensure_bridge_isolation')
assert fn_start != -1, 'ensure_bridge_isolation not found'
fn_body = src[fn_start:fn_start+4000]
masq_pos = fn_body.find('bridge_lan_masq')
drop_pos = fn_body.find('bridge_isolation_lan_wan')
assert masq_pos != -1, 'bridge_lan_masq anchor not found in ensure_bridge_isolation'
assert drop_pos != -1, 'bridge_isolation_lan_wan anchor not found in ensure_bridge_isolation'
assert masq_pos < drop_pos, f'ORDERING VIOLATION: bridge_lan_masq (pos {masq_pos}) must appear before bridge_isolation_lan_wan (pos {drop_pos}) in ensure_bridge_isolation body'
print('ordering assertion OK: MASQUERADE anchor precedes drop rule anchor')
"</automated>
    <manual>Confirm function appears after ensure_bridge_masquerade() and contains all 7 steps in order. Confirm ensure_bridge_masquerade() has DEPRECATED in its docstring.</manual>
  </verify>
  <done>
    - ensure_bridge_isolation() exists in linux_nftables.py with all 7 steps in correct order
    - MASQUERADE rule uses comment anchor "bridge_lan_masq"
    - Drop rules use comment anchors "bridge_isolation_lan_wan" and "bridge_isolation_wan_lan"
    - Drop rules applied via single nft -f - stdin batch (atomic)
    - Step 4 aborts (returns) if MASQUERADE fails — prevents SSH lockout
    - Step 6 deletes BRIDGE_MASQ_TABLE only after Step 5 completes
    - ensure_bridge_masquerade() marked DEPRECATED in docstring
    - Python syntax is valid
    - Ordering assertion confirms bridge_lan_masq anchor appears before bridge_isolation_lan_wan anchor within function body
  </done>
</task>

<task type="auto">
  <name>Task 2: Add remove_bridge_isolation() to linux_nftables.py</name>
  <files>backend/app/hal/linux_nftables.py</files>
  <action>
Add the async function remove_bridge_isolation() immediately after ensure_bridge_isolation().

Implementation using the existing handle-based deletion pattern (same as _delete_rule_by_handle() at line ~236):

Step 1 — Live handle lookup and deletion:
  out = await run_nft(["-a", "list", "chain", "bridge", "filter", "forward"], check=False)
  if out:
      for line in out.splitlines():
          if "bridge_isolation" in line and "handle" in line:
              handle_match = re.search(r"handle\s+(\d+)", line)
              if handle_match:
                  handle = handle_match.group(1)
                  await run_nft(
                      ["delete", "rule", "bridge", "filter", "forward", "handle", handle],
                      check=False,
                  )
                  logger.info(f"Removed bridge isolation rule handle {handle}")

  CRITICAL: Never cache handle numbers. Always re-query via nft -a list chain at call time. Handles change after Pi reboots when nftables.service reloads persisted rules.

Step 2 — Restore ICMP redirects:
  await _run_system_cmd(["sudo", "sysctl", "-w", "net.ipv4.conf.all.send_redirects=1"], check=False)
  await _run_system_cmd(["sudo", "sysctl", "-w", "net.ipv4.conf.br0.send_redirects=1"], check=False)

Step 3 — Persist:
  await persist_nftables()
  Log: "Bridge isolation removed — transparent bridge mode restored"

The function docstring must state: "Remove bridge isolation rules — return to transparent bridge mode. Uses live handle lookup — never caches handles (they change on reboot)."

Do NOT remove the MASQUERADE rule ("bridge_lan_masq") during rollback — it does not harm transparent bridge mode and removing it could affect other NAT rules.
Do NOT delete the masquerade_fix table during rollback — if rolling back, the masquerade_fix may need to be re-applied separately.
  </action>
  <verify>
    <automated>cd /e/Nextcloud-Yeni/TonbilAiFirewallV41 && python -c "import ast; ast.parse(open('backend/app/hal/linux_nftables.py').read()); print('syntax OK')" && grep -n "async def remove_bridge_isolation" backend/app/hal/linux_nftables.py && grep -n "bridge_isolation" backend/app/hal/linux_nftables.py | grep "handle" && grep -c "send_redirects=1" backend/app/hal/linux_nftables.py</automated>
    <manual>Confirm remove_bridge_isolation() appears after ensure_bridge_isolation(). Confirm it reads live handles (not cached). Confirm ICMP redirect restore uses send_redirects=1 (not 0).</manual>
  </verify>
  <done>
    - remove_bridge_isolation() exists in linux_nftables.py after ensure_bridge_isolation()
    - Uses live handle lookup via "nft -a list chain bridge filter forward"
    - Filters lines containing "bridge_isolation" + "handle" (comment anchor filter)
    - Deletes each matching handle with check=False
    - Restores send_redirects=1 for both all and br0
    - Calls persist_nftables()
    - Python syntax is valid
    - Both functions are importable: python -c "from app.hal.linux_nftables import ensure_bridge_isolation, remove_bridge_isolation"
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the following checks locally:

1. Python syntax valid:
   python -c "import ast; ast.parse(open('backend/app/hal/linux_nftables.py').read()); print('OK')"

2. Both functions present:
   grep -n "async def ensure_bridge_isolation\|async def remove_bridge_isolation" backend/app/hal/linux_nftables.py

3. All comment anchors present:
   grep -c "bridge_isolation_lan_wan\|bridge_isolation_wan_lan\|bridge_lan_masq" backend/app/hal/linux_nftables.py
   (expect 3 distinct strings)

4. Deprecation marker present:
   grep -n "DEPRECATED" backend/app/hal/linux_nftables.py

5. No hardcoded interface names in new functions (they use _get_wan_bridge_port()):
   Confirm wan_iface variable used for eth0 references in ensure_bridge_isolation()

6. Atomic batch pattern present:
   grep -n "NFT_BIN.*-f.*-\|create_subprocess_exec.*NFT_BIN" backend/app/hal/linux_nftables.py
   (at least 2 occurrences — one for MASQUERADE, one for drop rules)

7. Ordering assertion (MASQUERADE before drop rules in function body):
   python -c "
src = open('backend/app/hal/linux_nftables.py').read()
fn_start = src.find('async def ensure_bridge_isolation')
fn_body = src[fn_start:fn_start+4000]
masq_pos = fn_body.find('bridge_lan_masq')
drop_pos = fn_body.find('bridge_isolation_lan_wan')
assert masq_pos < drop_pos, 'ORDERING VIOLATION'
print('ordering OK')
"

On Pi (after deploying and restarting backend):
- sudo nft list chain bridge filter forward
  Expected: two rules with bridge_isolation_lan_wan and bridge_isolation_wan_lan comments
- sudo nft list chain inet nat postrouting
  Expected: rule with bridge_lan_masq comment
- sudo nft list tables
  Expected: "bridge masquerade_fix" NOT present
- curl -s --connect-timeout 5 -o /dev/null -w '%{http_code}' https://www.google.com
  Expected: 200
- sysctl net.ipv4.ip_forward
  Expected: net.ipv4.ip_forward = 1
</verification>

<success_criteria>
- ensure_bridge_isolation() and remove_bridge_isolation() exist in linux_nftables.py
- ensure_bridge_masquerade() is marked DEPRECATED (not removed)
- Python syntax is valid (ast.parse passes)
- 7-step ordering preserved in ensure_bridge_isolation() — MASQUERADE (Step 4) always before drop rules (Step 5)
- Ordering assertion (Python script) confirms bridge_lan_masq anchor precedes bridge_isolation_lan_wan within function body
- Six sysctl values applied in Steps 1-3 before any nft drop rule in Step 5
- Both drop rules applied in single atomic nft -f - transaction
- remove_bridge_isolation() uses live handle lookup (never cached handles)
- ICMP redirects restored to 1 in remove_bridge_isolation()
- All comment anchors present: bridge_isolation_lan_wan, bridge_isolation_wan_lan, bridge_lan_masq
- No hardcoded "eth0" or "192.168.1.0/24" in new functions (uses helpers)
</success_criteria>

<output>
After completion, create .planning/phases/01-bridge-isolation-core/01-01-SUMMARY.md with:
- What was implemented (function signatures, step count, key patterns used)
- Comment anchors used and their purpose
- Deprecation note on ensure_bridge_masquerade()
- Any deviations from this plan and why
- Verification results (syntax check output, grep results)
- Next phase reminder: Phase 2 rewrites bridge accounting chains (per_device → upload/download on input/output hooks)
</output>
