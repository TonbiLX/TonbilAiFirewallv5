---
phase: 13-communication-config
plan: 02
type: execute
wave: 2
depends_on: ['01']
files_modified:
  - android/app/src/main/java/com/tonbil/aifirewall/feature/telegram/TelegramScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/wifi/WifiScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/dhcp/DhcpScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/securitysettings/SecuritySettingsScreen.kt
autonomous: true
requirements: [TELE-01, TELE-02, WIFI-01, WIFI-02, WIFI-03, DHCP-01, DHCP-02, SEC-01, SEC-02]

must_haves:
  truths:
    - "Telegram bot token ve chat ID yapilandirilabiliyor"
    - "Telegram bildirim toggle'lari calisir durumda"
    - "WiFi AP durumu gorunuyor, SSID/sifre/kanal degistirilebiliyor, AP acilip kapatilabiliyor"
    - "WiFi AP kapatma islemi onay dialogu gosteriyor"
    - "DHCP havuz bilgileri gorunuyor ve statik IP atamalari yonetilebiliyor"
    - "DHCP statik lease silme islemi onay dialogu gosteriyor"
    - "Guvenlik esik degerleri gorunuyor ve degistirilebiliyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/telegram/TelegramScreen.kt"
      provides: "Telegram bot yapilandirma ve bildirim ayarlari ekrani"
      min_lines: 300
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/wifi/WifiScreen.kt"
      provides: "WiFi AP durumu, yapilandirma ve toggle ekrani"
      min_lines: 400
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/dhcp/DhcpScreen.kt"
      provides: "DHCP havuz bilgileri ve statik lease yonetimi ekrani"
      min_lines: 400
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/securitysettings/SecuritySettingsScreen.kt"
      provides: "Guvenlik ayarlari esik degerleri ekrani"
      min_lines: 500
  key_links:
    - from: "TelegramScreen.kt"
      to: "/api/v1/telegram"
      via: "Ktor HttpClient GET/POST"
      pattern: "ApiRoutes.*TELEGRAM|telegram"
    - from: "WifiScreen.kt"
      to: "/api/v1/wifi"
      via: "Ktor HttpClient GET/PUT"
      pattern: "ApiRoutes.*WIFI|wifi"
    - from: "DhcpScreen.kt"
      to: "/api/v1/dhcp"
      via: "Ktor HttpClient GET/POST/DELETE"
      pattern: "ApiRoutes.*DHCP|dhcp"
    - from: "SecuritySettingsScreen.kt"
      to: "/api/v1/security/config"
      via: "Ktor HttpClient GET/PUT + POST reload"
      pattern: "security.*config|security.*reload"
---

<objective>
Mevcut Telegram, WiFi, DHCP ve Guvenlik Ayarlari ekranlarini dogrula ve ince ayar yap.

Purpose: Arastirma sonucuna gore bu 4 ekran zaten tam fonksiyonel durumda. Bu plan ekranlarin derleme dogrulamasini yapar ve varsa kucuk UX iyilestirmeleri (onay dialoglari, MAC validasyonu, hata mesajlari) uygular. TELE-01/02, WIFI-01/02/03, DHCP-01/02, SEC-01/02 requirement'larini resmi olarak tamamlanmis isaret eder.

Output: Dogrulanmis ve (gerekiyorsa) ince ayar yapilmis ekranlar, basarili APK build
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-communication-config/13-RESEARCH.md

<interfaces>
<!-- Tum ekranlar mevcut ve fonksiyonel. Asagidaki bilgiler referans icin. -->

From android/app/src/main/java/com/tonbil/aifirewall/feature/telegram/:
- TelegramScreen.kt (343 satir) — bot token, chat ID alanlari, notify_threats/devices/ddos toggle
- TelegramViewModel.kt — loadConfig(), saveConfig(), testMessage()

From android/app/src/main/java/com/tonbil/aifirewall/feature/wifi/:
- WifiScreen.kt (474 satir) — WifiStatusCard (durum + toggle), WifiConfigCard (SSID/sifre/kanal edit)
- WifiViewModel.kt — loadStatus(), loadConfig(), saveConfig(), toggleAp()

From android/app/src/main/java/com/tonbil/aifirewall/feature/dhcp/:
- DhcpScreen.kt (457 satir) — DhcpStatsRow, pool listesi, AddStaticLeaseDialog, lease silme
- DhcpViewModel.kt — loadData(), addStaticLease(), deleteStaticLease()

From android/app/src/main/java/com/tonbil/aifirewall/feature/securitysettings/:
- SecuritySettingsScreen.kt (635 satir) — Slider esik degerleri, kaydet butonu, reload
- SecuritySettingsViewModel.kt — loadConfig(), updateConfig(), reloadConfig()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Telegram + WiFi ekranlari dogrulama ve ince ayar</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/telegram/TelegramScreen.kt,
    android/app/src/main/java/com/tonbil/aifirewall/feature/wifi/WifiScreen.kt
  </files>
  <action>
Her iki ekrani incele ve asagidaki kontrolleri yap:

**TelegramScreen (TELE-01, TELE-02):**
1. Bot token ve chat ID text field'larinin dogru calistigini dogrula (OutlinedTextField + viewModel binding)
2. Bildirim toggle'larinin (notify_threats, notify_devices, notify_ddos) Switch + onCheckedChange ile viewModel'a baglandigini dogrula
3. "Kaydet" butonunun saveConfig() cagirdigini, "Test Mesaji" butonunun testMessage() cagirdigini dogrula
4. Hata durumunda SnackbarHost veya error Text gosterildigini dogrula
5. Eksiklik varsa duzelT, yoksa dokunma

**WifiScreen (WIFI-01, WIFI-02, WIFI-03):**
1. WiFi AP durumunun (SSID, kanal, frekans, bagli istemci sayisi) WifiStatusCard'da goruntulendigini dogrula
2. AP toggle Switch'inin calisiyor oldugunu dogrula (toggleAp viewModel metodu)
3. Edit mode'da SSID, sifre, kanal alanlarinin degistirilebildigini dogrula
4. saveConfig() cagrisinin yapildigini dogrula
5. WiFi kapatildiginda uyari dialog'u olup olmadigini kontrol et — yoksa ekle (AP kapatmak cihazlari dusurur, kullanici onay vermeli):
   ```kotlin
   // AP kapatma oncesi onay dialog'u
   if (showDisableDialog) {
       AlertDialog(
           onDismissRequest = { showDisableDialog = false },
           title = { Text("WiFi AP Kapat", color = TextPrimary) },
           text = { Text("WiFi erisim noktasini kapatmak tum bagli cihazlarin baglantilarini keser. Devam edilsin mi?", color = TextSecondary) },
           confirmButton = { TextButton(onClick = { viewModel.toggleAp(); showDisableDialog = false }) { Text("Kapat", color = NeonRed) } },
           dismissButton = { TextButton(onClick = { showDisableDialog = false }) { Text("Vazgec", color = TextSecondary) } },
           containerColor = DarkSurface,
       )
   }
   ```
   Bu dialog'u sadece AP acik iken kapatma isleminde goster. AP acma icin dogrudan calistir.

Degisiklik YAPMA gerekliligi yoksa sadece kodu oku ve dogrula — bos commit yapma. Sadece gercek bir eksiklik veya iyilestirme varsa dosyayi degistir.
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>
    - TelegramScreen bot token/chat ID text field'lari OutlinedTextField ile viewModel'a bagli
    - TelegramScreen bildirim toggle'lari (notify_threats, notify_devices, notify_ddos) Switch bileseninde calisiyor
    - TelegramScreen "Kaydet" butonu saveConfig(), "Test Mesaji" butonu testMessage() cagiriyor
    - WifiScreen AP durumu (SSID, kanal, frekans, bagli istemci) WifiStatusCard'da gorunuyor
    - WifiScreen AP kapatma onay dialog'u mevcut (AlertDialog ile kullanici onay veriyor)
    - WifiScreen SSID/sifre/kanal edit mode calisiyor
    - APK basariyla derleniyor
  </done>
</task>

<task type="auto">
  <name>Task 2: DHCP + Guvenlik Ayarlari ekranlari dogrulama ve ince ayar</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/dhcp/DhcpScreen.kt,
    android/app/src/main/java/com/tonbil/aifirewall/feature/securitysettings/SecuritySettingsScreen.kt
  </files>
  <action>
Her iki ekrani incele ve asagidaki kontrolleri yap:

**DhcpScreen (DHCP-01, DHCP-02):**
1. DHCP havuz bilgilerinin (pool range, lease time, DNS server) DhcpStatsRow veya benzer bir composable'da goruntulendigini dogrula
2. Statik IP atamalari listesinin goruntulendigini dogrula
3. AddStaticLeaseDialog'un dogru alanlari icerdigini dogrula (MAC, IP, hostname)
4. Silme isleminin confirmation dialog ile yapildigini dogrula — yoksa ekle:
   ```kotlin
   // Silme oncesi onay
   if (showDeleteDialog != null) {
       AlertDialog(
           onDismissRequest = { showDeleteDialog = null },
           title = { Text("Statik Lease Sil", color = TextPrimary) },
           text = { Text("${showDeleteDialog} MAC adresli atama silinecek.", color = TextSecondary) },
           confirmButton = { TextButton(onClick = { viewModel.deleteStaticLease(showDeleteDialog!!); showDeleteDialog = null }) { Text("Sil", color = NeonRed) } },
           dismissButton = { TextButton(onClick = { showDeleteDialog = null }) { Text("Vazgec", color = TextSecondary) } },
           containerColor = DarkSurface,
       )
   }
   ```
5. MAC format kontrolu: DHCP static lease eklediginde MAC adresinin `AA:BB:CC:DD:EE:FF` formatinda oldugunu validate eden bir kontrol var mi? Yoksa basic regex ile dogrulama ekle: `Regex("^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$")`

**SecuritySettingsScreen (SEC-01, SEC-02):**
1. Guvenlik ayarlari ekraninin backend'den config yukleyip gosterdigini dogrula
2. Slider degerlerinin float'tan Int'e dogru yuvarlandigini dogrula (roundToInt) — RESEARCH'te belirtilen pitfall
3. "Kaydet" butonunun updateConfig() cagirdigini dogrula
4. "Canli Yukle" / "Reload" butonunun reloadConfig() cagirdigini dogrula (hot-reload tetikleme)
5. Slider etiketlerinin mevcut degeri gosterdigini dogrula (kullanici hangi deger sectigini gormeli)

Degisiklik YAPMA gerekliligi yoksa sadece kodu oku ve dogrula. Sadece gercek bir eksiklik veya iyilestirme varsa dosyayi degistir.
  </action>
  <verify>
    <automated>cd android && ./gradlew assembleDebug 2>&1 | tail -5</automated>
  </verify>
  <done>
    - DhcpScreen havuz bilgileri (pool range, lease time, DNS) goruntulenebiliyor
    - DhcpScreen statik lease listesi gorunuyor, AddStaticLeaseDialog MAC/IP/hostname alanlari iceriyor
    - DhcpScreen statik lease silme onay dialog'u mevcut (AlertDialog ile kullanici onay veriyor)
    - DhcpScreen MAC format validasyonu mevcut (AA:BB:CC:DD:EE:FF regex kontrolu)
    - SecuritySettingsScreen esik degerleri slider ile goruntulenebiliyor ve mevcut deger etikette gorunuyor
    - SecuritySettingsScreen slider float→int yuvarlamasi roundToInt ile dogru calisiyor
    - SecuritySettingsScreen "Kaydet" butonu updateConfig(), "Canli Yukle" butonu reloadConfig() cagiriyor
    - APK basariyla derleniyor
  </done>
</task>

</tasks>

<verification>
1. `cd android && ./gradlew assembleDebug` basarili
2. Tum 4 ekran (Telegram, WiFi, DHCP, SecuritySettings) kaynak kodda mevcut ve derleniyor
3. Requirement karsilama: TELE-01/02, WIFI-01/02/03, DHCP-01/02, SEC-01/02 tumu saglanmis
4. WifiScreen AP kapatma onay dialog'u mevcut
5. DhcpScreen silme onay dialog'u mevcut
</verification>

<success_criteria>
- Telegram, WiFi, DHCP ve Guvenlik Ayarlari ekranlari basariyla derleniyor
- Tum requirement'lar mevcut ekranlar tarafindan karsilaniyor
- WifiScreen AP kapatma oncesi onay dialog'u calisiyor
- DhcpScreen statik lease silme oncesi onay dialog'u calisiyor
- SecuritySettingsScreen slider float→int yuvarlamasi dogru
- APK basariyla derleniyor
</success_criteria>

<output>
After completion, create `.planning/phases/13-communication-config/13-02-SUMMARY.md`
</output>
