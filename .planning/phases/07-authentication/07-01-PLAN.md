---
phase: 07-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/gradle/libs.versions.toml
  - android/app/build.gradle.kts
  - android/app/src/main/java/com/tonbil/aifirewall/data/local/TokenManager.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/local/ServerConfig.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/AuthInterceptor.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/ServerDiscovery.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiClient.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiRoutes.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/AuthDtos.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/repository/AuthRepository.kt
  - android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "JWT token EncryptedSharedPreferences ile guvenli sekilde saklanabiliyor"
    - "Her API istegine otomatik olarak Bearer token ekleniyor"
    - "Yerel ag (192.168.1.2) ve uzak sunucu (wall.tonbilx.com) arasinda otomatik gecis yapiliyor"
    - "Baglanti kopuklugunda diger adres otomatik deneniyor"
    - "Sunucu URL'si kaydedilip geri okunabiliyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/local/TokenManager.kt"
      provides: "Encrypted token CRUD operations"
      exports: ["TokenManager"]
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/ServerDiscovery.kt"
      provides: "Auto-discovery local vs remote URL switching"
      exports: ["ServerDiscovery"]
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/repository/AuthRepository.kt"
      provides: "Login API call + token persistence"
      exports: ["AuthRepository"]
  key_links:
    - from: "data/remote/AuthInterceptor.kt"
      to: "data/local/TokenManager.kt"
      via: "reads token for Bearer header"
      pattern: "tokenManager\\.getToken"
    - from: "data/remote/ApiClient.kt"
      to: "data/remote/ServerDiscovery.kt"
      via: "gets active base URL"
      pattern: "serverDiscovery\\.getActiveUrl"
    - from: "data/repository/AuthRepository.kt"
      to: "data/local/TokenManager.kt"
      via: "saves token after login"
      pattern: "tokenManager\\.saveToken"
---

<objective>
Auth veri katmani: guvenli token depolama, sunucu kesfetme (yerel/uzak otomatik gecis), auth interceptor ve login repository

Purpose: Phase 7'nin UI katmaninin ihtiyac duydugu tum veri altyapisini kurar. Login ekrani ve biyometrik auth bu katman uzerine insa edilecek.
Output: TokenManager, ServerDiscovery, AuthRepository, AuthInterceptor — tumu Koin ile inject edilebilir
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-project-skeleton/06-02-SUMMARY.md

<interfaces>
<!-- Mevcut koddan alinmis arayuzler -->

From data/remote/ApiRoutes.kt:
```kotlin
object ApiRoutes {
    const val BASE_URL = "https://wall.tonbilx.com/api/v1/"
    const val LOCAL_URL = "http://192.168.1.2/api/v1/"
    const val AUTH_LOGIN = "auth/login"
    const val DASHBOARD_SUMMARY = "dashboard/summary"
    const val DEVICES = "devices"
    const val WS_URL = "wss://wall.tonbilx.com/api/v1/ws"
}
```

From data/remote/ApiClient.kt:
```kotlin
fun createHttpClient(): HttpClient {
    return HttpClient(OkHttp) {
        install(ContentNegotiation) { json(...) }
        install(Logging) { level = LogLevel.HEADERS }
        install(HttpTimeout) { requestTimeoutMillis = 15_000; connectTimeoutMillis = 10_000 }
        defaultRequest { url(ApiRoutes.BASE_URL); contentType(ContentType.Application.Json) }
    }
}
```

From di/AppModule.kt:
```kotlin
val appModule = module { single { createHttpClient() } }
val featureModules = module {
    viewModel { DashboardViewModel(get()) }
    viewModel { DevicesViewModel() }
    viewModel { SecurityViewModel() }
    viewModel { SettingsViewModel() }
}
```

Backend POST /api/v1/auth/login request:
```json
{"username": "string", "password": "string", "remember_me": false}
```

Backend POST /api/v1/auth/login response:
```json
{"access_token": "jwt_string", "token_type": "bearer", "username": "admin", "display_name": "Admin"}
```

Backend GET /api/v1/auth/me response (requires Bearer token):
```json
{"id": 1, "username": "admin", "display_name": "Admin", "is_admin": true}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token depolama + Sunucu yapilandirma + DTO'lar</name>
  <files>
    android/gradle/libs.versions.toml
    android/app/build.gradle.kts
    android/app/src/main/java/com/tonbil/aifirewall/data/local/TokenManager.kt
    android/app/src/main/java/com/tonbil/aifirewall/data/local/ServerConfig.kt
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/AuthDtos.kt
  </files>
  <action>
1. **libs.versions.toml** — Yeni dependency ekle:
   - `security-crypto = "1.1.0-alpha06"` (EncryptedSharedPreferences)
   - `biometric = "1.2.0-alpha05"` (Plan 02'de kullanilacak, simdi ekle)
   - `datastore-preferences = "1.1.4"` (sunucu ayarlari icin)
   Libraries bolumune ekle:
   - `security-crypto = { group = "androidx.security", name = "security-crypto", version.ref = "securityCrypto" }`
   - `biometric = { group = "androidx.biometric", name = "biometric", version.ref = "biometric" }`
   - `datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastorePreferences" }`

2. **app/build.gradle.kts** — dependencies bolumune ekle:
   - `implementation(libs.security.crypto)`
   - `implementation(libs.biometric)`
   - `implementation(libs.datastore.preferences)`

3. **TokenManager.kt** (data/local/) — EncryptedSharedPreferences ile JWT token yonetimi:
   - `saveToken(token: String)` — tokeni sifrelenmis olarak kaydet
   - `getToken(): String?` — tokeni oku
   - `clearToken()` — tokeni sil
   - `saveUserInfo(username: String, displayName: String?)` — kullanici bilgisi kaydet
   - `getUserInfo(): Pair<String, String?>?` — kullanici bilgisi oku
   - `isLoggedIn(): Boolean` — token var mi kontrol et
   - `setBiometricEnabled(enabled: Boolean)` / `isBiometricEnabled(): Boolean`
   - EncryptedSharedPreferences kullan: `MasterKey.Builder(context).setKeyScheme(MasterKey.KeyScheme.AES256_GCM).build()`
   - SharedPreferences dosya adi: `"tonbilai_secure_prefs"`

4. **ServerConfig.kt** (data/local/) — DataStore Preferences ile sunucu URL yonetimi:
   - `serverUrlFlow: Flow<String>` — kaydedilmis sunucu URL'si (varsayilan: ApiRoutes.BASE_URL)
   - `setServerUrl(url: String)` — sunucu URL kaydet
   - `isOnboardingCompleted(): Flow<Boolean>` — onboarding tamamlandi mi
   - `setOnboardingCompleted()` — onboarding tamamlandi isaretle
   - `getLastConnectedUrl(): Flow<String?>` — son basarili baglanti URL'si
   - `setLastConnectedUrl(url: String)` — son basarili baglanti URL kaydet
   - DataStore dosya adi: `"server_config"`
   - Key'ler: `SERVER_URL`, `ONBOARDING_COMPLETED`, `LAST_CONNECTED_URL`

5. **AuthDtos.kt** (data/remote/dto/) — Backend API ile eslesen @Serializable data class'lar:
   ```kotlin
   @Serializable
   data class LoginRequest(
       val username: String,
       val password: String,
       @SerialName("remember_me") val rememberMe: Boolean = true
   )

   @Serializable
   data class LoginResponse(
       @SerialName("access_token") val accessToken: String,
       @SerialName("token_type") val tokenType: String = "bearer",
       val username: String,
       @SerialName("display_name") val displayName: String? = null
   )

   @Serializable
   data class UserInfo(
       val id: Int,
       val username: String,
       @SerialName("display_name") val displayName: String? = null,
       @SerialName("is_admin") val isAdmin: Boolean = false
   )
   ```
   NOT: rememberMe her zaman true (mobil uygulamada uzun oturum mantikli)
  </action>
  <verify>
    <automated>cd android && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - EncryptedSharedPreferences ile token CRUD calisiyor
    - DataStore ile sunucu URL persist ediliyor
    - DTO'lar backend auth endpoint'leri ile eslesiyor
    - Proje hatasiz derleniyor
  </done>
</task>

<task type="auto">
  <name>Task 2: Server discovery + Auth interceptor + AuthRepository + DI baglama</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/ServerDiscovery.kt
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/AuthInterceptor.kt
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiClient.kt
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiRoutes.kt
    android/app/src/main/java/com/tonbil/aifirewall/data/repository/AuthRepository.kt
    android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
  </files>
  <action>
1. **ServerDiscovery.kt** (data/remote/) — Yerel/uzak sunucu otomatik gecis:
   - Constructor: `(serverConfig: ServerConfig, httpClient: HttpClient)` — DIKKAT: Koin lazy inject ile dairesel bagimliligi onle
   - `suspend fun getActiveUrl(): String` — once kaydedilmis/son basarili URL'yi dene, basarisizsa diger adresi dene
   - `suspend fun testConnection(url: String): Boolean` — GET `{url}dashboard/summary` ile baglanti testi (try-catch, 5s timeout)
   - `suspend fun discoverServer(): String?` — Sirasıyla dene:
     1. `ServerConfig.lastConnectedUrl` (son basarili)
     2. `ApiRoutes.LOCAL_URL` (yerel ag — 192.168.1.2)
     3. `ApiRoutes.BASE_URL` (uzak — wall.tonbilx.com)
     Ilk basarili olan URL'yi `ServerConfig.setLastConnectedUrl()` ile kaydet ve dondur
   - `activeUrl: String` property — cache'lenmis aktif URL (discover sonucu)
   - `suspend fun switchToUrl(url: String)` — manuel URL degistirme (sunucu ayarlari ekranindan)
   - Baglanti testi icin ayri bir basit HttpClient kullan (interceptor'suz, kisa timeout: connectTimeout=3s, requestTimeout=5s)

2. **AuthInterceptor.kt** (data/remote/) — Ktor HttpClient plugin olarak:
   - Ktor `createClientPlugin("AuthInterceptor")` kullanarak olustur
   - `onRequest` fazinda: `tokenManager.getToken()` varsa `Authorization: Bearer {token}` header ekle
   - `onResponse` fazinda: 401 donerse `tokenManager.clearToken()` yap (token expired)
   - Mutex kullanma (token refresh yok — backend 24 saat gecerli token veriyor, expired olunca login ekranina yonlendir)

3. **ApiClient.kt** guncelle — Mevcut `createHttpClient()` fonksiyonunu degistir:
   - `fun createHttpClient(serverDiscovery: ServerDiscovery, tokenManager: TokenManager): HttpClient`
   - AuthInterceptor plugin'ini install et
   - `defaultRequest` icinde URL'yi `serverDiscovery.activeUrl` kullan (statik ApiRoutes.BASE_URL yerine)
   - NOT: `createTestHttpClient(): HttpClient` — interceptor'suz, kisa timeout'lu test client'i ekle (ServerDiscovery icin)

4. **ApiRoutes.kt** guncelle — Yeni endpoint sabitler ekle:
   - `const val AUTH_ME = "auth/me"`
   - `const val AUTH_LOGOUT = "auth/logout"`
   - `const val AUTH_CHECK = "auth/check"`
   - WS_URL'yi dinamik yapma — WebSocket Phase 8'de ele alinacak

5. **AuthRepository.kt** (data/repository/) — Auth is mantigi:
   - Constructor: `(httpClient: HttpClient, tokenManager: TokenManager)`
   - `suspend fun login(username: String, password: String): Result<LoginResponse>` — POST auth/login, basarili ise tokenManager.saveToken + saveUserInfo
   - `suspend fun getCurrentUser(): Result<UserInfo>` — GET auth/me (token gecerliligi kontrolu)
   - `suspend fun logout(): Result<Unit>` — POST auth/logout + tokenManager.clearToken()
   - `fun isLoggedIn(): Boolean` — tokenManager.isLoggedIn() delegate
   - `fun getStoredUserInfo(): Pair<String, String?>?` — tokenManager.getUserInfo() delegate
   - Tum API cagrilari try-catch ile sarilanmis, Result.success/Result.failure donduruyor

6. **AppModule.kt** guncelle — Yeni DI tanimlari:
   ```kotlin
   val appModule = module {
       single { TokenManager(androidContext()) }
       single { ServerConfig(androidContext()) }
       single { createTestHttpClient() }  // named ile ayir
       single { ServerDiscovery(get(), get<HttpClient>(named("test"))) }
       single { createHttpClient(get(), get()) }
       single { AuthRepository(get(), get()) }
   }
   ```
   DIKKAT: Test HttpClient ve ana HttpClient ayri Koin tanimlari olmali.
   Named qualifier kullan: `single(named("test")) { createTestHttpClient() }` ve
   `single { ServerDiscovery(get(), get(named("test"))) }`
  </action>
  <verify>
    <automated>cd android && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ServerDiscovery yerel/uzak gecis yapiyor (LOCAL_URL ve BASE_URL arasinda)
    - AuthInterceptor her istege Bearer token ekliyor
    - AuthRepository login/logout/me API cagrilarini yapiyor
    - Tum yeni siniflar Koin ile inject edilebiliyor
    - Proje hatasiz derleniyor
  </done>
</task>

</tasks>

<verification>
- `cd android && ./gradlew compileDebugKotlin` hatasiz tamamlaniyor
- TokenManager, ServerConfig, ServerDiscovery, AuthInterceptor, AuthRepository siniflarinin tumu mevcut
- AppModule'de tum yeni siniflar Koin tanimlarina eklenmis
- ApiClient artik serverDiscovery'den dinamik URL aliyor
</verification>

<success_criteria>
- Proje derleniyor, yeni dependency'ler (security-crypto, biometric, datastore) cozulmus
- Token CRUD islemleri EncryptedSharedPreferences ile calisiyor
- Sunucu URL DataStore ile persist ediliyor
- API client dinamik URL ve auth interceptor ile yapilandirilmis
- AuthRepository login/logout/me islemleri icin hazir
</success_criteria>

<output>
After completion, create `.planning/phases/07-authentication/07-01-SUMMARY.md`
</output>
