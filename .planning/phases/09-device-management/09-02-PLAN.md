---
phase: 09-device-management
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DevicesViewModel.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DevicesScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt
  - android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
  - android/app/src/main/java/com/tonbil/aifirewall/ui/navigation/AppNavHost.kt
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-04, DEV-05]

must_haves:
  truths:
    - "Kullanici cihaz listesinde her cihazin ismi, IP'si, durumu ve anlik bant genisligini goruyor"
    - "Kullanici tek dokunusla bir cihazin internetini durdurabiliyor/geri acabiliyor"
    - "Kullanici cihaz detayinda trafik gecmisi, DNS sorgulari ve profil bilgisi gorebiliyor"
    - "Kullanici cihaza profil atayabiliyor/degistirebiliyor"
    - "Tum cihaz ekranlarinda asagiya cekerek yenileme calisiyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DevicesViewModel.kt"
      provides: "Device list state, refresh, block/unblock, WS bandwidth merge"
      min_lines: 80
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DevicesScreen.kt"
      provides: "Device list UI with pull-to-refresh, device cards, block toggle"
      min_lines: 120
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt"
      provides: "Device detail state, tabs data, profile assign"
      min_lines: 80
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt"
      provides: "Device detail UI with tabs: overview, traffic, DNS"
      min_lines: 150
  key_links:
    - from: "DevicesViewModel"
      to: "DeviceRepository + WebSocketManager"
      via: "Koin injection, REST load + WS bandwidth collect"
      pattern: "deviceRepository\\.getDevices|webSocketManager\\.messages\\.collect"
    - from: "DeviceDetailViewModel"
      to: "DeviceRepository + ProfileRepository"
      via: "Koin injection, REST calls"
      pattern: "deviceRepository\\.getDevice|profileRepository\\.getProfiles"
    - from: "AppNavHost"
      to: "DevicesScreen + DeviceDetailScreen"
      via: "Navigation Compose composable routes"
      pattern: "composable<DevicesRoute>|composable<DeviceDetailRoute>"
    - from: "DevicesScreen"
      to: "DeviceDetailRoute"
      via: "navController.navigate on card click"
      pattern: "navigate.*DeviceDetailRoute"
---

<objective>
Device Management UI katmani: DevicesScreen (cihaz listesi, pull-to-refresh, block toggle, WS bandwidth), DeviceDetailScreen (3 tab: genel, trafik, DNS) ve navigation wiring

Purpose: Kullanicinin tum ag cihazlarini gorup yonetebilmesi — Phase 9 tamamlaniyor
Output: 4 dosya (2 ViewModel + 2 Screen), AppModule ve AppNavHost guncelleme
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard/08-02-SUMMARY.md
@.planning/phases/09-device-management/09-01-SUMMARY.md

<interfaces>
<!-- Plan 01'den gelen arayuzler (executor icin) -->

From data/remote/dto/DeviceDto.kt (Plan 01'de olusturuldu):
```kotlin
@Serializable
data class DeviceResponseDto(
    val id: Int,
    @SerialName("mac_address") val macAddress: String,
    @SerialName("ip_address") val ipAddress: String?,
    val hostname: String?,
    val manufacturer: String?,
    @SerialName("profile_id") val profileId: Int?,
    @SerialName("is_blocked") val isBlocked: Boolean,
    @SerialName("is_online") val isOnline: Boolean,
    @SerialName("first_seen") val firstSeen: String?,
    @SerialName("last_seen") val lastSeen: String?,
    @SerialName("total_online_seconds") val totalOnlineSeconds: Int,
    @SerialName("last_online_start") val lastOnlineStart: String?,
    @SerialName("bandwidth_limit_mbps") val bandwidthLimitMbps: Int?,
)

@Serializable
data class DeviceUpdateDto(
    val hostname: String? = null,
    @SerialName("ip_address") val ipAddress: String? = null,
    val manufacturer: String? = null,
    @SerialName("profile_id") val profileId: Int? = null,
)

@Serializable
data class BlockResponseDto(val status: String, val mac: String)
```

From data/remote/dto/ProfileDto.kt (Plan 01):
```kotlin
@Serializable
data class ProfileResponseDto(
    val id: Int, val name: String, val profileType: String?,
    val allowedHours: String?, val contentFilters: List<String>,
    val bandwidthLimitMbps: Int?,
)
```

From data/remote/dto/DnsQueryLogDto.kt (Plan 01):
```kotlin
@Serializable
data class DnsQueryLogDto(
    val domain: String, val queryType: String, val blocked: Boolean,
    val timestamp: String, val clientIp: String, val responseIp: String?,
)
```

From data/remote/dto/TrafficFlowDto.kt (Plan 01):
```kotlin
@Serializable
data class DeviceTrafficSummaryDto(
    val deviceId: Int, val totalFlows: Int, val activeFlows: Int,
    val totalBytesIn: Long, val totalBytesOut: Long, val topServices: List<TopServiceDto>,
)
data class TopServiceDto(val service: String, val bytes: Long)
```

From data/remote/dto/ConnectionHistoryDto.kt (Plan 01):
```kotlin
@Serializable
data class ConnectionHistoryDto(
    val deviceId: Int, val connectedAt: String?, val disconnectedAt: String?,
    val timestamp: String?, val durationSeconds: Int?,
)
```

From data/repository/DeviceRepository.kt (Plan 01):
```kotlin
class DeviceRepository(private val client: HttpClient) {
    suspend fun getDevices(...): Result<List<DeviceResponseDto>>
    suspend fun getDevice(id: Int): Result<DeviceResponseDto>
    suspend fun updateDevice(id: Int, update: DeviceUpdateDto): Result<DeviceResponseDto>
    suspend fun blockDevice(id: Int): Result<BlockResponseDto>
    suspend fun unblockDevice(id: Int): Result<BlockResponseDto>
    suspend fun getConnectionHistory(id: Int, limit: Int = 20): Result<List<ConnectionHistoryDto>>
    suspend fun getDnsLogs(clientIp: String, limit: Int = 50): Result<List<DnsQueryLogDto>>
    suspend fun getTrafficSummary(id: Int): Result<DeviceTrafficSummaryDto>
}
```

From data/repository/ProfileRepository.kt (Plan 01):
```kotlin
class ProfileRepository(private val client: HttpClient) {
    suspend fun getProfiles(): Result<List<ProfileResponseDto>>
}
```

From data/remote/WebSocketManager.kt (Phase 08):
```kotlin
class WebSocketManager(...) {
    val messages: SharedFlow<RealtimeUpdateDto>
    fun connect()
    fun disconnect()
}
```

From data/remote/dto/WebSocketDto.kt (Phase 08):
```kotlin
data class WsBandwidthDto(
    val totalUploadBps: Long, val totalDownloadBps: Long,
    val devices: Map<String, WsDeviceBandwidthDto>,
)
data class WsDeviceBandwidthDto(val uploadBps: Long, val downloadBps: Long, val uploadTotal: Long, val downloadTotal: Long)
```

From ui/navigation/NavRoutes.kt:
```kotlin
@Serializable object DevicesRoute
@Serializable data class DeviceDetailRoute(val deviceId: String)
```

From ui/theme/Theme.kt (cyberpunk colors):
```kotlin
CyberpunkTheme.colors.neonCyan    // #00F0FF
CyberpunkTheme.colors.neonMagenta // #FF00E5
CyberpunkTheme.colors.neonGreen   // #39FF14
CyberpunkTheme.colors.neonAmber   // #FFB800
CyberpunkTheme.colors.neonRed     // #FF003C
CyberpunkTheme.colors.glassBg
CyberpunkTheme.colors.glassBorder
```

From feature/dashboard/DashboardViewModel.kt (pattern reference — dual source):
```kotlin
class DashboardViewModel(
    private val repository: DashboardRepository,
    private val webSocketManager: WebSocketManager,
) : ViewModel() {
    // REST loadSummary() on init + WS messages.collect for live
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DevicesViewModel + DevicesScreen (cihaz listesi, WS bandwidth, block toggle, pull-to-refresh)</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DevicesViewModel.kt,
    android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DevicesScreen.kt,
    android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
  </files>
  <action>
    **DevicesViewModel.kt** tamamen yeniden yaz (placeholder siliniyor):

    Constructor: `DevicesViewModel(private val deviceRepository: DeviceRepository, private val webSocketManager: WebSocketManager)`

    State (StateFlow):
    ```kotlin
    data class DevicesUiState(
        val devices: List<DeviceResponseDto> = emptyList(),
        val bandwidthMap: Map<String, WsDeviceBandwidthDto> = emptyMap(),
        val isLoading: Boolean = true,
        val isRefreshing: Boolean = false,
        val error: String? = null,
    )
    ```

    Fonksiyonlar:
    - `init`: viewModelScope.launch ile loadDevices() + WS bandwidth collect
    - `loadDevices()`: deviceRepository.getDevices() → state guncelle, isLoading=false
    - `refresh()`: isRefreshing=true, loadDevices(), isRefreshing=false (pull-to-refresh icin)
    - `toggleBlock(device: DeviceResponseDto)`: if isBlocked → unblockDevice(id) else blockDevice(id), sonra loadDevices() ile listeyi yenile
    - WS collect: webSocketManager.messages.collect { update -> bandwidthMap = update.bandwidth.devices }

    **DevicesScreen.kt** tamamen yeniden yaz:

    Parametreler: `viewModel: DevicesViewModel = koinViewModel(), onNavigateToDetail: (String) -> Unit`

    UI yapisi:
    1. Material3 pullToRefresh modifier ile LazyColumn sarmalama:
       ```kotlin
       val pullToRefreshState = rememberPullToRefreshState()
       Box(Modifier.pullToRefresh(isRefreshing, pullToRefreshState, onRefresh = { viewModel.refresh() })) {
           // content
           PullToRefreshDefaults.Indicator(state, isRefreshing, ...)
       }
       ```
    2. Loading durumu: CircularProgressIndicator (neonCyan) ortada
    3. Error durumu: GlassCard icinde hata mesaji + "Tekrar Dene" butonu
    4. Device listesi: LazyColumn icinde her cihaz icin GlassCard:
       - Sol: Durum gostergesi (yesil daire is_online=true, gri is_online=false)
       - Orta: hostname (veya "Bilinmeyen Cihaz"), ip_address altta kucuk yazi, manufacturer varsa ustune
       - Sag ust: Anlik bandwidth (bandwidthMap'ten device id key ile), formatBps helper kullan (DashboardScreen'deki pattern)
       - Sag alt: Block/unblock toggle butonu — IconButton ile:
         - is_blocked=true: Shield icon neonRed, tiklayinca unblock
         - is_blocked=false: ShieldCheck icon neonGreen, tiklayinca block
       - Kart tiklama: onNavigateToDetail(device.id.toString())
    5. Baslik: "Cihazlar" text + online cihaz sayisi badge

    Cyberpunk tema: Background koyu, GlassCard kartlar, neon renkler (yesil=online, kirmizi=blocked, cyan=baslik).

    **AppModule.kt** guncelleme:
    featureModules icinde DevicesViewModel satirini guncelle:
    ```kotlin
    viewModel { DevicesViewModel(get(), get()) }
    ```

    formatBps helper'i DevicesScreen icinde tanimla (DashboardScreen'deki ile ayni mantik: Gbps/Mbps/Kbps/bps).
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Cihaz listesi pull-to-refresh ile yukleniyor, WS'den anlik bandwidth gorunuyor, block/unblock toggle calisiyor, kart tiklama detay ekranina yonlendiriyor</done>
</task>

<task type="auto">
  <name>Task 2: DeviceDetailViewModel + DeviceDetailScreen (3 tab: genel/trafik/DNS) + profil atama + AppNavHost wiring</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailViewModel.kt,
    android/app/src/main/java/com/tonbil/aifirewall/feature/devices/DeviceDetailScreen.kt,
    android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt,
    android/app/src/main/java/com/tonbil/aifirewall/ui/navigation/AppNavHost.kt
  </files>
  <action>
    **DeviceDetailViewModel.kt** yeni dosya olustur:

    Constructor: `DeviceDetailViewModel(private val deviceId: Int, private val deviceRepository: DeviceRepository, private val profileRepository: ProfileRepository, private val webSocketManager: WebSocketManager)`

    State:
    ```kotlin
    data class DeviceDetailUiState(
        val device: DeviceResponseDto? = null,
        val profiles: List<ProfileResponseDto> = emptyList(),
        val connectionHistory: List<ConnectionHistoryDto> = emptyList(),
        val dnsLogs: List<DnsQueryLogDto> = emptyList(),
        val trafficSummary: DeviceTrafficSummaryDto? = null,
        val bandwidth: WsDeviceBandwidthDto? = null,
        val isLoading: Boolean = true,
        val isRefreshing: Boolean = false,
        val error: String? = null,
        val selectedTab: Int = 0,
    )
    ```

    Fonksiyonlar:
    - `init`: loadAll()
    - `loadAll()`: Paralel olarak (coroutineScope veya async) device, profiles, connectionHistory, dnsLogs (device.ip_address ile), trafficSummary yukle
    - `refresh()`: isRefreshing=true, loadAll(), isRefreshing=false
    - `selectTab(index: Int)`: selectedTab guncelle
    - `assignProfile(profileId: Int?)`: deviceRepository.updateDevice(deviceId, DeviceUpdateDto(profileId=profileId)) → loadAll() ile yenile
    - `toggleBlock()`: device null degilse, if isBlocked → unblock else block, loadAll()
    - WS collect: webSocketManager.messages.collect → bandwidth = update.bandwidth.devices[deviceId.toString()]

    **DeviceDetailScreen.kt** yeni dosya olustur:

    Parametreler: `deviceId: String, viewModel: DeviceDetailViewModel = koinViewModel(), onBack: () -> Unit`

    UI yapisi (tam ekran, ScrollableTabRow ile 3 tab):

    1. Ust bar: Geri butonu + cihaz ismi + block/unblock IconButton
    2. Pull-to-refresh ile sarmalama (ayni pattern)
    3. **Tab 0 - Genel Bakis:**
       - Cihaz bilgi karti: hostname, IP, MAC, manufacturer, durum (online/offline badge), son gorunme
       - Anlik bandwidth karti: upload/download bps (WS'den)
       - Profil secimi: Mevcut profil gosterimi + ExposedDropdownMenuBox ile profil degistirme
         - Dropdown items: "Profil Yok" + profiles listesi
         - Secim yapildiginda assignProfile() cagir
       - Connection history listesi: Son 5 baglanti (connected_at, disconnected_at, duration)
    4. **Tab 1 - Trafik:**
       - Trafik ozet karti: total_flows, active_flows, total_bytes_in/out (formatBytes helper)
       - Top services listesi: topServices.forEach { service, bytes → GlassCard row }
    5. **Tab 2 - DNS Sorgulari:**
       - DNS log listesi: LazyColumn, her satir: domain, query_type badge, blocked (kirmizi/yesil badge), timestamp
       - Engellenen sayisi ozet (filtrelenmis count)

    Cyberpunk tema tutarli: GlassCard kartlar, neon renkler, cyberpunk badge'ler.
    formatBytes helper: GB/MB/KB/B formatlama.

    **AppModule.kt** guncelleme (featureModules'e ekle):
    ```kotlin
    viewModel { params -> DeviceDetailViewModel(params.get(), get(), get(), get()) }
    ```

    **AppNavHost.kt** guncelleme:
    - DevicesRoute composable'ini guncelle: onNavigateToDetail parametresi ekle
      ```kotlin
      composable<DevicesRoute> {
          DevicesScreen(onNavigateToDetail = { deviceId ->
              navController.navigate(DeviceDetailRoute(deviceId))
          })
      }
      ```
    - DeviceDetailRoute composable'ini ekle:
      ```kotlin
      composable<DeviceDetailRoute> { backStackEntry ->
          val route = backStackEntry.toRoute<DeviceDetailRoute>()
          DeviceDetailScreen(
              deviceId = route.deviceId,
              onBack = { navController.popBackStack() },
          )
      }
      ```

    DeviceDetailViewModel'in Koin'den deviceId parametresi almasinin iki yolu:
    - Secim 1 (parametric): `viewModel { params -> DeviceDetailViewModel(params.get(), ...) }` ve Screen'de `koinViewModel { parametersOf(deviceId.toInt()) }`
    - Secim 2 (SavedStateHandle): ViewModel'de SavedStateHandle'dan alma
    Secim 1'i kullan (parametersOf) — daha basit ve mevcut pattern ile tutarli.
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>DeviceDetailScreen 3 tab ile calisiyor (genel/trafik/DNS), profil atama dropdown'u mevcut, pull-to-refresh tum cihaz ekranlarinda calisiyor, AppNavHost'ta DevicesRoute → DeviceDetailRoute navigasyonu bagli</done>
</task>

</tasks>

<verification>
- `./gradlew compileDebugKotlin` basarili
- DevicesScreen: LazyColumn + GlassCard kartlar + pull-to-refresh + block toggle
- DeviceDetailScreen: 3 tab (genel/trafik/DNS) + profil dropdown + pull-to-refresh
- AppNavHost: DevicesRoute → DevicesScreen(onNavigateToDetail), DeviceDetailRoute → DeviceDetailScreen
- AppModule: DevicesViewModel(get(), get()) ve DeviceDetailViewModel parametric Koin kaydi
- WS bandwidth: Hem listede hem detayda anlik hiz gorunuyor
</verification>

<success_criteria>
- Cihaz listesinde hostname, IP, online/offline durumu ve WS'den anlik bandwidth gorunuyor
- Cihaz kartina tiklaninca detay ekranina geciliyor (DeviceDetailRoute)
- Detay ekraninda 3 tab: genel bilgi + trafik ozet + DNS sorgulari
- Block/unblock toggle hem listede hem detayda calisiyor
- Profil atama dropdown ile cihaza profil atanabiliyor/degistirilebiliyor
- Pull-to-refresh DevicesScreen ve DeviceDetailScreen'de calisiyor
- Geri butonu ile cihaz listesine donulebiliyor
</success_criteria>

<output>
After completion, create `.planning/phases/09-device-management/09-02-SUMMARY.md`
</output>
