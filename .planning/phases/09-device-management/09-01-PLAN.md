---
phase: 09-device-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DeviceDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/ProfileDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DnsQueryLogDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/TrafficFlowDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/ConnectionHistoryDto.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiRoutes.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/repository/DeviceRepository.kt
  - android/app/src/main/java/com/tonbil/aifirewall/data/repository/ProfileRepository.kt
  - android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
autonomous: true
requirements: [DEV-01, DEV-02, DEV-03, DEV-04, DEV-05]

must_haves:
  truths:
    - "DeviceRepository cihaz listesini REST API'den basariyla cekerken Result pattern kullaniyor"
    - "Block/unblock islemleri DeviceRepository uzerinden yapilabiliyor"
    - "Profil listesi ve cihaza profil atama fonksiyonlari mevcut"
    - "Cihaz detay bilgileri (DNS log, trafik ozet, connection history) cekilebiliyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DeviceDto.kt"
      provides: "DeviceResponseDto, DeviceUpdateDto, BlockResponseDto"
      contains: "DeviceResponseDto"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/ProfileDto.kt"
      provides: "ProfileResponseDto"
      contains: "ProfileResponseDto"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/repository/DeviceRepository.kt"
      provides: "getDevices, getDevice, blockDevice, unblockDevice, updateDevice, getConnectionHistory, getDnsLogs, getTrafficSummary"
      exports: ["DeviceRepository"]
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/repository/ProfileRepository.kt"
      provides: "getProfiles"
      exports: ["ProfileRepository"]
  key_links:
    - from: "DeviceRepository"
      to: "ApiRoutes"
      via: "Ktor HttpClient GET/POST/PATCH"
      pattern: "client\\.(get|post|patch)\\(ApiRoutes"
    - from: "AppModule.kt"
      to: "DeviceRepository, ProfileRepository"
      via: "Koin single registration"
      pattern: "single.*DeviceRepository|single.*ProfileRepository"
---

<objective>
Device Management veri katmani: REST API DTO'lari, DeviceRepository (CRUD + block/unblock + detay verileri) ve ProfileRepository

Purpose: Plan 02 (UI katmani) icin tum veri operasyonlarini saglayan katman
Output: DTO'lar, repository'ler, Koin DI kayitlari
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard/08-01-SUMMARY.md

<interfaces>
<!-- Mevcut koddan executor'un ihtiyac duydugu arayuzler -->

From data/remote/ApiRoutes.kt:
```kotlin
object ApiRoutes {
    const val BASE_URL = "https://wall.tonbilx.com/api/v1/"
    const val LOCAL_URL = "http://192.168.1.2/api/v1/"
    const val DEVICES = "devices"
    // Yeni route'lar buraya eklenecek
}
```

From data/repository/DashboardRepository.kt (pattern reference):
```kotlin
class DashboardRepository(private val client: HttpClient) {
    suspend fun getSummary(): Result<DashboardSummaryDto> {
        return try {
            val response: DashboardSummaryDto = client.get(ApiRoutes.DASHBOARD_SUMMARY).body()
            Result.success(response)
        } catch (e: Exception) {
            Result.failure(e)
        }
    }
}
```

From di/AppModule.kt:
```kotlin
val appModule = module {
    single { DashboardRepository(get()) }
    // DeviceRepository ve ProfileRepository buraya eklenecek
}
val featureModules = module {
    viewModel { DevicesViewModel() }
    // DevicesViewModel parametreleri Plan 02'de guncellenecek
}
```

From data/remote/dto/WebSocketDto.kt (mevcut WS DTO'lari — UI'da bandwidth icin kullanilacak):
```kotlin
data class WsDeviceDto(val id: Int, val mac: String, val ip: String, val hostname: String?, val manufacturer: String?, val isOnline: Boolean)
data class WsDeviceBandwidthDto(val uploadBps: Long, val downloadBps: Long, val uploadTotal: Long, val downloadTotal: Long)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device/Profile/Detail DTO'lari ve ApiRoutes guncelleme</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DeviceDto.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/ProfileDto.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/DnsQueryLogDto.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/TrafficFlowDto.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/ConnectionHistoryDto.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/ApiRoutes.kt
  </files>
  <action>
    Backend API yapisina birebir eslesen DTO siniflarini olustur. Tum siniflar @Serializable ve @SerialName kullaniyor (mevcut WebSocketDto.kt pattern'ini takip et).

    **DeviceDto.kt:**
    - `DeviceResponseDto`: id (Int), mac_address (String), ip_address (String?), hostname (String?), manufacturer (String?), profile_id (Int?), is_blocked (Boolean), is_online (Boolean), first_seen (String?), last_seen (String?), total_online_seconds (Int), last_online_start (String?), bandwidth_limit_mbps (Int?)
    - `DeviceUpdateDto`: hostname (String?=null), ip_address (String?=null), manufacturer (String?=null), profile_id (Int?=null) — PATCH body icin
    - `BlockResponseDto`: status (String), mac (String)

    **ProfileDto.kt:**
    - `ProfileResponseDto`: id (Int), name (String), profile_type (String?), allowed_hours (String?=null), content_filters (List<String>), bandwidth_limit_mbps (Int?)

    **DnsQueryLogDto.kt:**
    - `DnsQueryLogDto`: domain (String), query_type (String), blocked (Boolean), timestamp (String), client_ip (String), response_ip (String?)

    **TrafficFlowDto.kt:**
    - `DeviceTrafficSummaryDto`: device_id (Int), total_flows (Int), active_flows (Int), total_bytes_in (Long), total_bytes_out (Long), top_services (List<TopServiceDto>)
    - `TopServiceDto`: service (String), bytes (Long)

    **ConnectionHistoryDto.kt:**
    - `ConnectionHistoryDto`: device_id (Int), connected_at (String?), disconnected_at (String?), timestamp (String?), duration_seconds (Int?)

    **ApiRoutes.kt guncelleme:**
    Mevcut DEVICES sabitine ek olarak:
    ```kotlin
    const val PROFILES = "profiles"
    const val DNS_QUERY_LOGS = "dns/query-logs"
    const val TRAFFIC_FLOWS_DEVICE = "traffic/flows/device"
    fun deviceDetail(id: Int) = "$DEVICES/$id"
    fun deviceBlock(id: Int) = "$DEVICES/$id/block"
    fun deviceUnblock(id: Int) = "$DEVICES/$id/unblock"
    fun deviceConnectionHistory(id: Int) = "$DEVICES/$id/connection-history"
    fun deviceTrafficSummary(id: Int) = "$TRAFFIC_FLOWS_DEVICE/$id/summary"
    ```
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>6 DTO dosyasi olusturulmus, ApiRoutes guncellenmis, proje derleniyor</done>
</task>

<task type="auto">
  <name>Task 2: DeviceRepository + ProfileRepository + Koin DI kayit</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/data/repository/DeviceRepository.kt,
    android/app/src/main/java/com/tonbil/aifirewall/data/repository/ProfileRepository.kt,
    android/app/src/main/java/com/tonbil/aifirewall/di/AppModule.kt
  </files>
  <action>
    DashboardRepository pattern'ini takip ederek (constructor'da HttpClient, Result pattern ile try/catch):

    **DeviceRepository.kt:**
    ```kotlin
    class DeviceRepository(private val client: HttpClient) {
        suspend fun getDevices(sortBy: String = "hostname", sortOrder: String = "asc"): Result<List<DeviceResponseDto>>
        // GET ApiRoutes.DEVICES?sort_by=$sortBy&sort_order=$sortOrder

        suspend fun getDevice(id: Int): Result<DeviceResponseDto>
        // GET ApiRoutes.deviceDetail(id)

        suspend fun updateDevice(id: Int, update: DeviceUpdateDto): Result<DeviceResponseDto>
        // PATCH ApiRoutes.deviceDetail(id) + setBody(update)

        suspend fun blockDevice(id: Int): Result<BlockResponseDto>
        // POST ApiRoutes.deviceBlock(id)

        suspend fun unblockDevice(id: Int): Result<BlockResponseDto>
        // POST ApiRoutes.deviceUnblock(id)

        suspend fun getConnectionHistory(id: Int, limit: Int = 20): Result<List<ConnectionHistoryDto>>
        // GET ApiRoutes.deviceConnectionHistory(id)?limit=$limit

        suspend fun getDnsLogs(clientIp: String, limit: Int = 50): Result<List<DnsQueryLogDto>>
        // GET ApiRoutes.DNS_QUERY_LOGS?client_ip=$clientIp&limit=$limit

        suspend fun getTrafficSummary(id: Int): Result<DeviceTrafficSummaryDto>
        // GET ApiRoutes.deviceTrafficSummary(id)
    }
    ```

    Ktor client.get/post/patch icin:
    - GET: `client.get(route) { url { parameters.append("key", "value") } }.body()`
    - POST: `client.post(route).body()` (block/unblock body-siz)
    - PATCH: `client.patch(route) { setBody(update) }.body()`

    **ProfileRepository.kt:**
    ```kotlin
    class ProfileRepository(private val client: HttpClient) {
        suspend fun getProfiles(): Result<List<ProfileResponseDto>>
        // GET ApiRoutes.PROFILES
    }
    ```

    **AppModule.kt guncelleme:**
    appModule icine ekle:
    ```kotlin
    single { DeviceRepository(get()) }
    single { ProfileRepository(get()) }
    ```
    featureModules'deki `viewModel { DevicesViewModel() }` satirini DEGISTIRME — bu Plan 02'de guncellenecek.
  </action>
  <verify>
    <automated>cd E:/Nextcloud-Yeni/TonbilAiFirevallv5/android && ./gradlew compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>DeviceRepository 8 metot, ProfileRepository 1 metot, Koin DI kayitlari tamamlanmis, proje derleniyor</done>
</task>

</tasks>

<verification>
- `./gradlew compileDebugKotlin` basarili
- DeviceDto.kt, ProfileDto.kt, DnsQueryLogDto.kt, TrafficFlowDto.kt, ConnectionHistoryDto.kt dosyalari mevcut
- DeviceRepository.kt ve ProfileRepository.kt dosyalari mevcut
- AppModule.kt icerisinde DeviceRepository ve ProfileRepository singleton kayitlari var
- ApiRoutes.kt icerisinde PROFILES, DNS_QUERY_LOGS, deviceDetail(), deviceBlock(), deviceUnblock() fonksiyonlari var
</verification>

<success_criteria>
- Proje hatasiz derleniyor
- Tum backend endpoint'lerine karsilik gelen DTO ve repository metodlari mevcut
- Result pattern tutarli sekilde uygulanmis (DashboardRepository ile ayni yaklasim)
- Koin DI kayitlari tamamlanmis
</success_criteria>

<output>
After completion, create `.planning/phases/09-device-management/09-01-SUMMARY.md`
</output>
