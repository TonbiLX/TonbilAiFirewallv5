---
phase: 11-network-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/SecurityDtos.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/security/SecurityViewModel.kt
  - android/app/src/main/java/com/tonbil/aifirewall/feature/security/SecurityScreen.kt
autonomous: true
requirements: [FW-01, FW-02, FW-03, VPN-01, VPN-02, VPN-03, VPN-04]

must_haves:
  truths:
    - "Firewall kural listesinde priority degeri gorunuyor ve kuralllar priority sirasina gore listeleniyor"
    - "Kullanici mevcut bir firewall kuralina dokunarak edit dialog acabilir ve kurali guncelleyebilir"
    - "Kullanici up/down butonlari ile firewall kural onceligi degistirebilir"
    - "VPN peer QR dialog'unda Paylas butonu var ve Android ShareSheet aciyor"
    - "VPN global durum gostergesi (aktif/pasif) VPN tab basinda gorunuyor"
  artifacts:
    - path: "android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/SecurityDtos.kt"
      provides: "FirewallRuleDto ve FirewallRuleCreateDto priority + description alanlari"
      contains: "val priority: Int"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/security/SecurityViewModel.kt"
      provides: "updateFirewallRule, moveRuleUp, moveRuleDown, showEditFirewallRuleDialog fonksiyonlari"
      contains: "fun moveRuleUp"
    - path: "android/app/src/main/java/com/tonbil/aifirewall/feature/security/SecurityScreen.kt"
      provides: "Edit dialog modu, priority up/down butonlari, VPN ShareSheet, VPN durum gostergesi"
      contains: "Intent.ACTION_SEND"
  key_links:
    - from: "SecurityScreen.kt (FirewallTab)"
      to: "SecurityViewModel.updateFirewallRule"
      via: "onUpdate callback in AddFirewallRuleDialog"
      pattern: "viewModel\\.updateFirewallRule"
    - from: "SecurityScreen.kt (VpnPeerConfigDialog)"
      to: "Android ShareSheet"
      via: "Intent.createChooser"
      pattern: "Intent\\.ACTION_SEND"
---

<objective>
Firewall kural duzenleme + oncelik siralama ve VPN QR paylasma ekle

Purpose: SecurityScreen'deki mevcut Firewall ve VPN tab'larini tam CRUD + UX iyilestirmesi ile tamamla
Output: Firewall edit dialog + priority up/down butonlari, VPN ShareSheet entegrasyonu, VPN durum gostergesi
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-network-security/11-RESEARCH.md

<interfaces>
<!-- Mevcut DTO'lar ve Repository metodlari -->

From SecurityDtos.kt (MEVCUT — genisletilecek):
```kotlin
@Serializable
data class FirewallRuleDto(
    val id: Int = 0,
    val name: String = "",
    val direction: String = "",
    val protocol: String = "",
    val port: String? = null,
    @SerialName("source_ip") val sourceIp: String? = null,
    @SerialName("dest_ip") val destIp: String? = null,
    val action: String = "",
    val enabled: Boolean = true,
    // EKSIK: priority, description
)

@Serializable
data class FirewallRuleCreateDto(
    val name: String,
    val direction: String,
    val protocol: String,
    val port: String? = null,
    @SerialName("source_ip") val sourceIp: String? = null,
    @SerialName("dest_ip") val destIp: String? = null,
    val action: String,
    val enabled: Boolean = true,
    // EKSIK: priority, description
)

@Serializable
data class VpnPeerConfigDto(
    @SerialName("config_text") val configText: String = "",
    @SerialName("qr_code_base64") val qrCodeBase64: String? = null,
)
```

From SecurityViewModel.kt (MEVCUT — genisletilecek):
```kotlin
data class SecurityUiState(
    // ... mevcut alanlar ...
    val showAddFirewallRuleDialog: Boolean = false,
    val showVpnPeerConfigDialog: String? = null,
    val vpnPeerConfig: VpnPeerConfigDto? = null,
    // EKSIK: editingFirewallRule
)

// Mevcut metodlar:
fun createFirewallRule(dto: FirewallRuleCreateDto)
fun toggleFirewallRule(id: Int)
fun deleteFirewallRule(id: Int)
fun showVpnPeerConfig(name: String)
// EKSIK: updateFirewallRule, moveRuleUp, moveRuleDown, showEditFirewallRuleDialog
```

From SecurityRepository.kt (MEVCUT — updateFirewallRule ZATEN VAR):
```kotlin
suspend fun updateFirewallRule(id: Int, dto: FirewallRuleCreateDto): Result<FirewallRuleDto>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DTO guncelleme + ViewModel genisletme (Firewall edit + priority + VPN durum)</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/data/remote/dto/SecurityDtos.kt,
    android/app/src/main/java/com/tonbil/aifirewall/feature/security/SecurityViewModel.kt
  </files>
  <action>
1. SecurityDtos.kt'de FirewallRuleDto'ya 2 alan ekle:
   - `val priority: Int = 100`
   - `val description: String? = null`
   FirewallRuleCreateDto'ya da ayni alanlari ekle:
   - `val priority: Int = 100`
   - `val description: String? = null`

2. SecurityViewModel.kt'de SecurityUiState'e yeni alan ekle:
   - `val editingFirewallRule: FirewallRuleDto? = null`

3. SecurityViewModel'a yeni metodlar ekle:
   - `showEditFirewallRuleDialog(rule: FirewallRuleDto)`: showAddFirewallRuleDialog=true, editingFirewallRule=rule ile state guncelle
   - `hideAddFirewallRuleDialog()` GUNCELLE: editingFirewallRule=null da ekle (dialog kapaninca edit modunu temizle)
   - `updateFirewallRule(id: Int, dto: FirewallRuleCreateDto)`: repository.updateFirewallRule cagir, basari/hata mesaji goster, refresh(). Pattern: mevcut createFirewallRule ile ayni (isActionLoading, dialog kapat, refresh)
   - `moveRuleUp(rule: FirewallRuleDto)`: mevcut firewallRules listesini priority'ye gore sirala, hedef kuralin bir ustundeki kuralim priority degerlerini swap et (iki ayri updateFirewallRule cagrisi), sonra refresh()
   - `moveRuleDown(rule: FirewallRuleDto)`: ayni mantik ama bir asagidaki kural ile swap

4. firewallRules listesinin loadAll'da veya state'de .sortedBy { it.priority } ile siralanmasini sagla. loadAll icindeki mevcut fwRules satirini degistirmek yerine, state copy'de `firewallRules = fwRules.await().getOrElse { emptyList() }.sortedBy { it.priority }` kullan.
  </action>
  <verify>
    <automated>cd android && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>FirewallRuleDto/CreateDto'da priority+description alanlari var, SecurityUiState'de editingFirewallRule var, ViewModel'da updateFirewallRule/moveRuleUp/moveRuleDown/showEditFirewallRuleDialog metodlari var, firewallRules priority'ye gore siralanmis</done>
</task>

<task type="auto">
  <name>Task 2: UI genisletme — Firewall edit dialog + priority butonlar + VPN ShareSheet + VPN durum</name>
  <files>
    android/app/src/main/java/com/tonbil/aifirewall/feature/security/SecurityScreen.kt
  </files>
  <action>
1. **AddFirewallRuleDialog'a edit modu ekle:**
   - Fonksiyon parametrelerine ekle: `editingRule: FirewallRuleDto? = null`, `onUpdate: (Int, FirewallRuleCreateDto) -> Unit = { _, _ -> }`
   - Dialog acildiginda editingRule != null ise tum alanlari (name, direction, protocol, port, sourceIp, destIp, action, priority) pre-populate et (remember icinde initialValue olarak)
   - Dialog basligini degistir: editingRule varsa "Kural Duzenle", yoksa "Yeni Kural Ekle"
   - Priority alani icin OutlinedTextField ekle (NumberKeyboard, varsayilan 100)
   - Kaydet butonunda: editingRule != null ise onUpdate(editingRule.id, dto) cagir, yoksa onCreate(dto) cagir

2. **FirewallTab'daki kural listesinde her satira:**
   - Edit butonu (Pencil/Edit ikonu) ekle — tiklaninca viewModel.showEditFirewallRuleDialog(rule) cagir
   - Up/Down ok butonlari (ArrowUp/ArrowDown ikonlari) ekle — tiklaninca viewModel.moveRuleUp(rule) / viewModel.moveRuleDown(rule) cagir
   - Priority degerini kural kartinda kucuk gri text olarak goster: "Oncelik: ${rule.priority}"
   - Butonlar: IconButton boyutunda, neon-cyan rengi, Row icinde yan yana

3. **AddFirewallRuleDialog cagrisini guncelle:**
   - `editingRule = uiState.editingFirewallRule` parametresini ekle
   - `onUpdate = { id, dto -> viewModel.updateFirewallRule(id, dto) }` callback'ini ekle

4. **VPN QR dialog'una (VpnPeerConfigDialog) Paylas butonu ekle:**
   - `val context = LocalContext.current` tanimla (Composable fonksiyon icinde)
   - Mevcut butonlarin yanina "Paylas" butonu ekle
   - Tiklandiginda: `Intent(Intent.ACTION_SEND)` olustur, `putExtra(Intent.EXTRA_TEXT, config.configText)`, `type = "text/plain"`, `context.startActivity(Intent.createChooser(sendIntent, "WireGuard Config"))` cagir
   - Buton icin Share/Send ikonu + "Paylas" text

5. **VPN tab basina global durum gostergesi ekle:**
   - VpnTab'in en ustune bir GlassCard veya Row ekle
   - vpnStats.serverEnabled true ise: yesil nokta + "VPN Aktif" + port + connected peers bilgisi
   - serverEnabled false ise: kirmizi nokta + "VPN Pasif"
   - Mevcut start/stop butonlari zaten var, onlari korumak yeterli

6. **Import'lari ekle:** `android.content.Intent`, `androidx.compose.ui.platform.LocalContext` (VPN ShareSheet icin)
  </action>
  <verify>
    <automated>cd android && ./gradlew :app:compileDebugKotlin 2>&1 | tail -5</automated>
  </verify>
  <done>Firewall kurallarinda edit butonu + duzenleme dialog'u calisiyor, up/down priority butonlari var, VPN QR dialog'unda Paylas butonu var ve ShareSheet aciyor, VPN tab basinda durum gostergesi var</done>
</task>

</tasks>

<verification>
- `cd android && ./gradlew :app:assembleDebug` basariyla APK uretiyor
- SecurityDtos.kt'de FirewallRuleDto `priority` alani mevcut
- SecurityViewModel.kt'de `updateFirewallRule`, `moveRuleUp`, `moveRuleDown` metodlari mevcut
- SecurityScreen.kt'de `Intent.ACTION_SEND` kullanimi mevcut (VPN ShareSheet)
- SecurityScreen.kt'de `editingRule` parametresi AddFirewallRuleDialog'da mevcut
</verification>

<success_criteria>
- Firewall kural listesi priority sirasina gore gorunuyor (FW-01)
- Firewall kuralina dokunarak edit dialog aciliyor ve kural guncellenebiliyor (FW-02)
- Up/down butonlari ile kural onceligi degistirilebiliyor (FW-03)
- VPN peer listesi gorunuyor (VPN-01, mevcut)
- VPN peer eklenebiliyor/silinebiliyor (VPN-02, mevcut)
- VPN durum gostergesi tab basinda gorunuyor (VPN-03)
- VPN QR dialog'unda Paylas butonu var ve ShareSheet aciyor (VPN-04)
</success_criteria>

<output>
After completion, create `.planning/phases/11-network-security/11-01-SUMMARY.md`
</output>
