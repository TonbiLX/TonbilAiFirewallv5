[pytest]
; Run all async test functions automatically without requiring
; an explicit @pytest.mark.asyncio on every coroutine.
asyncio_mode = auto

; Make the "app" package importable from the backend root.
pythonpath = .

testpaths = tests
